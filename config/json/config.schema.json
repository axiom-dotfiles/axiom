{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Axiom Configuration Schema",
  "description": "Source of truth for config/user/config.json. Every key's `default` is filled in at load time (SchemaValidation.applyDefaults), so config readers never need their own fallbacks. `title`/`description`/`minimum`/`maximum`/`enum` drive the generated settings UI; `x-settings: false` hides a key from it (edited elsewhere, or UI-owned state); `x-options` names a dynamic option list (\"screens\", \"chatProviders\" and \"chatPresets\", \"colors\" for the 16 base theme colors, \"languages\" from config/i18n, \"cursorThemes\" for installed cursor themes, \"edgeMenus\" for the ids of EdgeMenus); `x-showIf` ({ sibling: value | [values] | { not: value } }) hides a field unless a sibling matches; a key starting with `/` is a dotted path from the config root instead (\"/Workspaces.layout\"). `x-enumLabels` ({ value: label }) shows labels instead of raw enum values in dropdowns. `x-multiline: true` edits a string in a growing text area. `x-order` on an object schema lists the keys its form shows first, in order (a schema reaches QML as a map, whose keys come out sorted). `x-shapes` on an overlay module definition lists the slot shapes it fits (\"square\", \"horizontal\", \"vertical\"; a slot's shape comes from its span in OverlayConfig.layouts), and is enforced by the overlay editor; `x-icon` on a module or view definition is its Material Symbols icon name in the editor. `x-hosts` on a module definition lists where it may be placed (\"overlay\", \"edgeMenu\"; both when left out). Bar widgets are a oneOf discriminated by `type`, and get their `properties` defaults filled like everything else.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string", "x-settings": false },
    "version": {
      "type": "integer",
      "default": 15,
      "description": "Config format version, used for automatic migration.",
      "x-settings": false
    },

    "Lockscreen": {
      "type": "object",
      "x-category": "Desktop",
      "title": "Lockscreen",
      "description": "Which locker locks the session. Point your idle daemon's lock command at `qs -c axiom ipc call lockscreen lock` for the built-in or hyprlock modes.",
      "additionalProperties": false,
      "properties": {
        "mode": {
          "type": "string",
          "title": "Locker",
          "enum": ["quickshell", "hyprlock", "none"],
          "x-enumLabels": { "quickshell": "Built-in (Quickshell)", "hyprlock": "hyprlock, themed by axiom", "none": "None (bring your own)" },
          "default": "quickshell",
          "description": "Built-in uses the Wayland session-lock protocol with PAM, like hyprlock. hyprlock mode runs hyprlock with a config generated from the axiom theme. None leaves locking to your own locker."
        },
        "lockCommand": {
          "type": "string",
          "title": "Lock command",
          "default": "loginctl lock-session",
          "description": "What axiom's lock buttons run when the locker is your own.",
          "x-showIf": { "mode": "none" }
        },
        "showMedia": {
          "type": "boolean",
          "title": "Media controls",
          "default": true,
          "description": "Show the playing track and its controls on the lock screen.",
          "x-showIf": { "mode": "quickshell" }
        },
        "blurWallpaper": {
          "type": "boolean",
          "title": "Blur wallpaper",
          "default": true,
          "description": "Blur the wallpaper behind the hyprlock screen.",
          "x-showIf": { "mode": "hyprlock" }
        }
      }
    },

    "General": {
      "type": "object",
      "x-category": "Desktop",
      "title": "General",
      "additionalProperties": false,
      "properties": {
        "language": {
          "type": "string",
          "title": "Language",
          "default": "en",
          "description": "Language for the shell's text, dates and clock. Each file in config/i18n adds one.",
          "x-options": "languages"
        },
        "displayName": {
          "type": "string",
          "title": "Display name",
          "default": "",
          "description": "Name shown on the lockscreen and in chat. Empty uses your login name."
        },
        "primaryMonitor": {
          "type": "string",
          "title": "Primary monitor",
          "default": "",
          "description": "Monitor that hosts the lockscreen input, and the surfaces below when they're on one monitor. Empty uses the first screen.",
          "x-options": "screens"
        },
        "monitors": {
          "type": "string",
          "title": "Monitors",
          "enum": ["primary", "focused", "all"],
          "x-enumLabels": { "primary": "Primary monitor only", "focused": "Focused monitor only", "all": "Every monitor" },
          "default": "primary",
          "description": "Where the overlay, launcher, power menu and OSD appear (each can override this). Focused monitor opens them on the one with your active workspace (and mouse); every monitor opens them on all of them at once. The workspace overview always opens on the monitor under the mouse."
        }
      }
    },

    "Launcher": {
      "type": "object",
      "x-category": "Desktop",
      "title": "Launcher",
      "description": "The search launcher: apps, open windows, a calculator, web search, running commands, and / commands that control the shell.",
      "additionalProperties": false,
      "properties": {
        "monitors": {
          "type": "string",
          "title": "Monitors",
          "enum": ["general", "primaryBar", "focused", "all"],
          "x-enumLabels": { "general": "As in General", "primaryBar": "Primary bar's monitor", "focused": "Focused monitor", "all": "Every monitor" },
          "default": "general",
          "description": "Where the launcher opens. As in General follows General > Monitors; the primary bar's monitor is the first bar's; the focused monitor is the one with your active workspace (and mouse); every monitor shows it on all of them at once."
        },
        "position": {
          "type": "string",
          "title": "Position",
          "enum": ["center", "upper", "top", "bottom"],
          "x-enumLabels": { "center": "Center", "upper": "Upper third", "top": "Top edge", "bottom": "Bottom edge" },
          "default": "upper",
          "description": "Where on the screen the launcher opens. On the top or bottom edge it grows out of the border or bar there, like the other edge popouts."
        },
        "reverse": { "type": "boolean", "title": "Search bar at the bottom", "default": false, "description": "Put the search field under the results, with the best match nearest to it. Suits the bottom edge." },
        "width": { "type": "integer", "title": "Width", "x-unit": "px", "default": 680, "minimum": 400, "maximum": 1200, "description": "Width of the launcher." },
        "maxResults": { "type": "integer", "title": "Results shown", "default": 8, "minimum": 3, "maximum": 20, "description": "How many rows fit before the list scrolls." },
        "iconSize": { "type": "integer", "title": "Icon size", "x-unit": "px", "default": 32, "minimum": 16, "maximum": 64, "description": "Size of the result icons." },
        "showBackdrop": { "type": "boolean", "title": "Dim background", "default": true, "description": "Dim the whole screen behind the launcher, under the bars and border." },
        "backdrop": { "type": "integer", "title": "Backdrop darkness", "x-unit": "%", "default": 25, "minimum": 0, "maximum": 90, "description": "How dark the screen behind the launcher goes.", "x-showIf": { "showBackdrop": true } },
        "showDescriptions": { "type": "boolean", "title": "Descriptions", "default": true, "description": "Show a second line under each result (an app's generic name, a window's class, a command's description)." },
        "showRecent": { "type": "boolean", "title": "Frequent apps when empty", "default": true, "description": "List your most used apps before you type anything." },
        "showHint": { "type": "boolean", "title": "Controls hint", "default": true, "description": "Show the search prefixes and keys under the results." },
        "hiddenApps": { "type": "array", "title": "Hidden apps", "default": [], "items": { "type": "string" }, "description": "Desktop entry ids never shown, comma-separated (e.g. avahi-discover, qv4l2)." },
        "windows": { "type": "boolean", "title": "Open windows", "default": true, "description": "Also match open windows by title and class; picking one focuses it." },
        "calculator": { "type": "boolean", "title": "Calculator", "default": true, "description": "Show the result of math as you type it (or after =), using qalc. Enter copies the result." },
        "commands": { "type": "boolean", "title": "Shell commands (/)", "default": true, "description": "Type / for commands that control the shell: themes, volume, wifi, bluetooth, overlay pages, session and more." },
        "runCommands": { "type": "boolean", "title": "Run commands (>)", "default": true, "description": "Type > and a command to run it. Shift+Enter runs it in the terminal." },
        "terminal": { "type": "string", "title": "Terminal", "default": "kitty", "description": "Terminal used for commands run with Shift+Enter and for terminal apps. It's started as: terminal -e command." },
        "webSearch": { "type": "boolean", "title": "Web search (?)", "default": true, "description": "Offer a web search for what you typed, last in the list (or first after ?)." },
        "searchEngine": { "type": "string", "title": "Search engine URL", "default": "https://duckduckgo.com/?q={}", "description": "{} is replaced by the search terms.", "x-showIf": { "webSearch": true } },
        "chat": { "type": "boolean", "title": "Ask chat (@)", "default": true, "description": "Type @ and a question to ask it in the overlay's Chat module, with the default preset." }
      }
    },

    "PowerMenu": {
      "type": "object",
      "x-category": "Desktop",
      "title": "Power menu",
      "description": "The power menu: lock, suspend, hibernate, log out, reboot and power off, opened by the power button, a keybind or `qs -c axiom ipc call powermenu toggle`.",
      "additionalProperties": false,
      "properties": {
        "monitors": {
          "type": "string",
          "title": "Monitors",
          "enum": ["general", "primaryBar", "focused", "all"],
          "x-enumLabels": { "general": "As in General", "primaryBar": "Primary bar's monitor", "focused": "Focused monitor", "all": "Every monitor" },
          "default": "general",
          "description": "Where the power menu opens. As in General follows General > Monitors; the primary bar's monitor is the first bar's; the focused monitor is the one with your active workspace (and mouse); every monitor shows it on all of them at once."
        },
        "actions": { "type": "array", "title": "Actions", "default": ["lock", "suspend", "hibernate", "logout", "reboot", "poweroff"], "items": { "type": "string", "enum": ["lock", "suspend", "hibernate", "logout", "reboot", "poweroff"] }, "description": "Which actions the menu shows, in order." },
        "confirm": { "type": "boolean", "title": "Confirm destructive actions", "default": true, "description": "Log out, reboot and power off ask for a second click (or Enter) first." },
        "tileSize": { "type": "integer", "title": "Tile size", "x-unit": "px", "default": 120, "minimum": 80, "maximum": 200, "description": "Width and height of each action's tile." },
        "showHeader": { "type": "boolean", "title": "Greeting and uptime", "default": true, "description": "Show a greeting and how long the computer has been up above the actions." },
        "showHint": { "type": "boolean", "title": "Controls hint", "default": true, "description": "Show the keys under the actions." },
        "showBackdrop": { "type": "boolean", "title": "Dim background", "default": true, "description": "Dim the whole screen behind the power menu, under the bars and border." },
        "backdrop": { "type": "integer", "title": "Backdrop darkness", "x-unit": "%", "default": 35, "minimum": 0, "maximum": 90, "description": "How dark the screen behind the power menu goes.", "x-showIf": { "showBackdrop": true } }
      }
    },

    "Notifications": {
      "type": "object",
      "x-category": "Desktop",
      "title": "Notifications",
      "description": "Where notification toasts appear, how long they stay, and what the history keeps. Toasts sit inside the work area, the space the bars and screen border leave free, so a gap is measured from whatever is at that edge: a bar, the border's stroke, or the bare screen edge.",
      "additionalProperties": false,
      "x-order": ["monitors", "position", "evenGaps", "gap", "gapTop", "gapBottom", "gapLeft", "gapRight", "timeout", "appTimeouts", "criticalStays", "maxToasts", "width", "quietFullscreen", "clearOnFocus", "removeOnClick", "closeRemoves", "maxEntries", "maxAgeDays"],
      "properties": {
        "monitors": {
          "type": "string",
          "title": "Monitors",
          "enum": ["general", "primaryBar", "focused", "all"],
          "x-enumLabels": { "general": "As in General", "primaryBar": "Primary bar's monitor", "focused": "Focused monitor", "all": "Every monitor" },
          "default": "general",
          "description": "Where toasts appear. As in General follows General > Monitors; the primary bar's monitor is the first bar's; the focused monitor is the one with your active workspace when the notification arrives; every monitor shows each toast on all of them, and closing one closes the rest."
        },
        "position": {
          "type": "string",
          "title": "Corner",
          "enum": ["topLeft", "topRight", "bottomLeft", "bottomRight"],
          "x-enumLabels": { "topLeft": "Top left", "topRight": "Top right", "bottomLeft": "Bottom left", "bottomRight": "Bottom right" },
          "default": "topLeft",
          "description": "Corner of the work area the toasts stack from. Bottom corners stack upwards."
        },
        "evenGaps": { "type": "boolean", "title": "Even gaps", "default": true, "description": "Use one gap for both sides of the corner. Turn off to set each side on its own." },
        "gap": { "type": "integer", "title": "Gap", "x-unit": "px", "default": 12, "minimum": 0, "maximum": 200, "description": "Space between the toasts and the edges of the work area.", "x-showIf": { "evenGaps": true } },
        "gapTop": { "type": "integer", "title": "Top gap", "x-unit": "px", "default": 12, "minimum": 0, "maximum": 200, "description": "Space between the toasts and the top of the work area.", "x-showIf": { "evenGaps": false, "position": ["topLeft", "topRight"] } },
        "gapBottom": { "type": "integer", "title": "Bottom gap", "x-unit": "px", "default": 12, "minimum": 0, "maximum": 200, "description": "Space between the toasts and the bottom of the work area.", "x-showIf": { "evenGaps": false, "position": ["bottomLeft", "bottomRight"] } },
        "gapLeft": { "type": "integer", "title": "Left gap", "x-unit": "px", "default": 12, "minimum": 0, "maximum": 200, "description": "Space between the toasts and the left of the work area.", "x-showIf": { "evenGaps": false, "position": ["topLeft", "bottomLeft"] } },
        "gapRight": { "type": "integer", "title": "Right gap", "x-unit": "px", "default": 12, "minimum": 0, "maximum": 200, "description": "Space between the toasts and the right of the work area.", "x-showIf": { "evenGaps": false, "position": ["topRight", "bottomRight"] } },
        "timeout": { "type": "integer", "title": "Toast duration", "x-unit": "s", "x-group": "Toasts", "default": 5, "minimum": 1, "maximum": 60, "description": "How long a toast stays up. Hovering it pauses the countdown." },
        "appTimeouts": { "type": "boolean", "title": "Use app timeouts", "x-group": "Toasts", "default": true, "description": "When an app asks for its own duration, or for its notification to stay until dismissed, use that instead of the duration above." },
        "criticalStays": { "type": "boolean", "title": "Critical toasts stay", "x-group": "Toasts", "default": true, "description": "Critical notifications stay up until you dismiss them." },
        "maxToasts": { "type": "integer", "title": "Visible toasts", "x-group": "Toasts", "default": 5, "minimum": 1, "maximum": 10, "description": "How many toasts stack up at once. A new one pushes out the oldest." },
        "width": { "type": "integer", "title": "Toast width", "x-unit": "px", "x-group": "Toasts", "default": 360, "minimum": 240, "maximum": 600, "description": "Width of each toast." },
        "quietFullscreen": { "type": "boolean", "title": "Quiet over fullscreen", "x-group": "Toasts", "default": true, "description": "Show no toasts on a monitor whose workspace has a fullscreen window. Critical ones still show, and everything still goes to the history." },
        "clearOnFocus": { "type": "boolean", "title": "Clear when the app is focused", "x-group": "Notification history", "default": false, "description": "Focusing an app's window removes its notifications from the history." },
        "removeOnClick": { "type": "boolean", "title": "Remove when clicked", "x-group": "Notification history", "default": false, "description": "Clicking a notification, or one of its actions, removes it from the history." },
        "closeRemoves": { "type": "boolean", "title": "Closing a toast removes it", "x-group": "Notification history", "default": false, "description": "Closing a toast (its close button or a swipe) also removes the notification from the history. Off, it only hides the toast." },
        "maxEntries": { "type": "integer", "title": "History size", "x-group": "Notification history", "default": 100, "minimum": 10, "maximum": 500, "description": "How many notifications the history keeps. The oldest go first." },
        "maxAgeDays": { "type": "integer", "title": "Keep for", "x-unit": "days", "x-group": "Notification history", "default": 0, "minimum": 0, "maximum": 365, "description": "Remove notifications older than this many days. 0 keeps them until removed." }
      }
    },

    "Hyprland": {
      "type": "object",
      "x-category": "Hyprland",
      "x-links": ["Keybinds"],
      "x-card": "HyprlandStatus",
      "title": "Hyprland",
      "description": "How axiom sets Hyprland up. Needs Hyprland 0.55+ with a Lua config.",
      "additionalProperties": false,
      "properties": {
        "mode": {
          "type": "string",
          "title": "Mode",
          "x-group": "Setup",
          "enum": ["detached", "included", "managed"],
          "x-enumLabels": { "detached": "Detached (nothing to set up)", "included": "Included from your hyprland.lua", "managed": "Managed by axiom" },
          "default": "detached",
          "x-applyOnSave": true,
          "description": "Applies on Save. The status card says what the change will do."
        },
        "requiredSettings": {
          "type": "boolean",
          "title": "Required settings",
          "x-group": "Setup",
          "default": true,
          "description": "Lock restore after a shell restart, and a workspace slide animation."
        },
        "wallpaperDaemon": {
          "type": "boolean",
          "title": "Start awww",
          "x-group": "Setup",
          "default": true,
          "description": "Start awww-daemon for the wallpaper picker."
        },
        "theme": {
          "type": "boolean",
          "title": "Themed borders",
          "x-group": "Integration",
          "default": false,
          "description": "Window borders in theme colors."
        },
        "blur": {
          "type": "boolean",
          "title": "Blur behind axiom",
          "x-group": "Integration",
          "default": false,
          "description": "Blur behind bars, popouts and the launcher."
        },
        "binds": {
          "type": "array",
          "title": "Keybinds",
          "x-settings": false,
          "description": "Edited on the Keybinds page.",
          "default": [
            { "key": "SUPER + space", "action": "launcher" },
            { "key": "SUPER + Tab", "action": "overlay" },
            { "key": "SUPER + SHIFT + space", "action": "workspaceOverview" },
            { "key": "SUPER + Escape", "action": "powerMenu" },
            { "key": "SUPER + CTRL + L", "action": "lock" },
            { "key": "SUPER + 1", "action": "workspaceNth", "argument": "1" },
            { "key": "SUPER + SHIFT + 1", "action": "moveWindowNth", "argument": "1" },
            { "key": "SUPER + 2", "action": "workspaceNth", "argument": "2" },
            { "key": "SUPER + SHIFT + 2", "action": "moveWindowNth", "argument": "2" },
            { "key": "SUPER + 3", "action": "workspaceNth", "argument": "3" },
            { "key": "SUPER + SHIFT + 3", "action": "moveWindowNth", "argument": "3" },
            { "key": "SUPER + 4", "action": "workspaceNth", "argument": "4" },
            { "key": "SUPER + SHIFT + 4", "action": "moveWindowNth", "argument": "4" },
            { "key": "SUPER + 5", "action": "workspaceNth", "argument": "5" },
            { "key": "SUPER + SHIFT + 5", "action": "moveWindowNth", "argument": "5" },
            { "key": "SUPER + 6", "action": "workspaceNth", "argument": "6" },
            { "key": "SUPER + SHIFT + 6", "action": "moveWindowNth", "argument": "6" },
            { "key": "SUPER + 7", "action": "workspaceNth", "argument": "7" },
            { "key": "SUPER + SHIFT + 7", "action": "moveWindowNth", "argument": "7" },
            { "key": "SUPER + 8", "action": "workspaceNth", "argument": "8" },
            { "key": "SUPER + SHIFT + 8", "action": "moveWindowNth", "argument": "8" },
            { "key": "SUPER + 9", "action": "workspaceNth", "argument": "9" },
            { "key": "SUPER + SHIFT + 9", "action": "moveWindowNth", "argument": "9" },
            { "key": "SUPER + 0", "action": "workspaceNth", "argument": "10" },
            { "key": "SUPER + SHIFT + 0", "action": "moveWindowNth", "argument": "10" },
            { "key": "SUPER + H", "action": "focusDir", "argument": "left" },
            { "key": "SUPER + SHIFT + H", "action": "moveWindowDir", "argument": "left" },
            { "key": "SUPER + J", "action": "focusDir", "argument": "down" },
            { "key": "SUPER + SHIFT + J", "action": "moveWindowDir", "argument": "down" },
            { "key": "SUPER + K", "action": "focusDir", "argument": "up" },
            { "key": "SUPER + SHIFT + K", "action": "moveWindowDir", "argument": "up" },
            { "key": "SUPER + L", "action": "focusDir", "argument": "right" },
            { "key": "SUPER + SHIFT + L", "action": "moveWindowDir", "argument": "right" },
            { "key": "SUPER + ALT + H", "action": "resizeWindow", "argument": "-50 0", "repeating": true },
            { "key": "SUPER + ALT + J", "action": "resizeWindow", "argument": "0 50", "repeating": true },
            { "key": "SUPER + ALT + K", "action": "resizeWindow", "argument": "0 -50", "repeating": true },
            { "key": "SUPER + ALT + L", "action": "resizeWindow", "argument": "50 0", "repeating": true },
            { "key": "SUPER + C", "action": "closeWindow" },
            { "key": "SUPER + F", "action": "fullscreen" },
            { "key": "SUPER + Z", "action": "toggleFloat" },
            { "key": "SUPER + mouse:272", "action": "mouseDrag" },
            { "key": "SUPER + mouse:273", "action": "mouseResize" },
            { "key": "XF86AudioRaiseVolume", "action": "volumeUp", "locked": true, "repeating": true },
            { "key": "XF86AudioLowerVolume", "action": "volumeDown", "locked": true, "repeating": true },
            { "key": "XF86AudioMute", "action": "toggleMute", "locked": true },
            { "key": "XF86AudioMicMute", "action": "toggleMicMute", "locked": true },
            { "key": "XF86MonBrightnessUp", "action": "brightnessUp", "locked": true, "repeating": true },
            { "key": "XF86MonBrightnessDown", "action": "brightnessDown", "locked": true, "repeating": true },
            { "key": "XF86AudioPlay", "action": "mediaPlayPause", "locked": true },
            { "key": "XF86AudioPause", "action": "mediaPlayPause", "locked": true },
            { "key": "XF86AudioNext", "action": "mediaNext", "locked": true },
            { "key": "XF86AudioPrev", "action": "mediaPrevious", "locked": true }
          ],
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "key": { "type": "string", "title": "Key", "default": "", "description": "Modifiers and key, joined by +: \"SUPER + SHIFT + space\"." },
              "action": {
                "type": "string",
                "title": "Action",
                "enum": ["launcher", "launcherSearch", "overlay", "overlayPage", "edgeMenu", "workspaceOverview", "powerMenu", "lock", "toggleDnd", "clearNotifications", "idleInhibit", "workspaceStep", "moveWindowStep", "moveWindowStepSilent", "workspaceNth", "moveWindowNth", "moveWindowNthSilent", "focusDir", "moveWindowDir", "resizeWindow", "closeWindow", "fullscreen", "toggleFloat", "pin", "centerWindow", "toggleGroup", "mouseDrag", "mouseResize", "toggleSpecial", "moveToSpecial", "volumeUp", "volumeDown", "toggleMute", "toggleMicMute", "brightnessUp", "brightnessDown", "mediaPlayPause", "mediaNext", "mediaPrevious", "exec"],
                "x-enumLabels": {
                  "launcher": "App launcher",
                  "launcherSearch": "Launcher with text",
                  "overlay": "Overlay",
                  "overlayPage": "Overlay page",
                  "edgeMenu": "Edge menu",
                  "workspaceOverview": "Workspace overview",
                  "powerMenu": "Power menu",
                  "lock": "Lock",
                  "toggleDnd": "Do not disturb",
                  "clearNotifications": "Clear notifications",
                  "idleInhibit": "Caffeine",
                  "workspaceStep": "Workspace in a direction",
                  "moveWindowStep": "Move window in a direction",
                  "workspaceNth": "Workspace by number",
                  "moveWindowStepSilent": "Send window in a direction",
                  "moveWindowNth": "Move window to number",
                  "moveWindowNthSilent": "Send window to number",
                  "focusDir": "Focus in a direction",
                  "moveWindowDir": "Move window within the workspace",
                  "resizeWindow": "Resize window",
                  "closeWindow": "Close window",
                  "fullscreen": "Fullscreen",
                  "toggleFloat": "Toggle floating",
                  "pin": "Pin window",
                  "centerWindow": "Center window",
                  "toggleGroup": "Toggle window group",
                  "mouseDrag": "Drag window (mouse button)",
                  "mouseResize": "Resize window (mouse button)",
                  "toggleSpecial": "Toggle special workspace",
                  "moveToSpecial": "Move window to special workspace",
                  "volumeUp": "Volume up",
                  "volumeDown": "Volume down",
                  "toggleMute": "Mute",
                  "toggleMicMute": "Mute microphone",
                  "brightnessUp": "Brightness up",
                  "brightnessDown": "Brightness down",
                  "mediaPlayPause": "Play/Pause",
                  "mediaNext": "Next track",
                  "mediaPrevious": "Previous track",
                  "exec": "Run a command"
                },
                "default": "launcher"
              },
              "argument": {
                "type": "string",
                "title": "Argument",
                "default": "",
                "description": "Search text, page, direction, workspace number, special workspace name, resize step (\"50 0\") or command.",
                "x-showIf": { "action": ["launcherSearch", "overlayPage", "edgeMenu", "workspaceStep", "moveWindowStep", "moveWindowStepSilent", "workspaceNth", "moveWindowNth", "moveWindowNthSilent", "focusDir", "moveWindowDir", "resizeWindow", "toggleSpecial", "moveToSpecial", "exec"] }
              },
              "repeating": { "type": "boolean", "title": "Repeat while held", "default": false },
              "locked": { "type": "boolean", "title": "Works while locked", "default": false },
              "release": { "type": "boolean", "title": "On release", "default": false },
              "description": { "type": "string", "title": "Description", "default": "", "description": "Label on the Keybinds page; empty uses the action's name. The action picks the section unless you write \"Section: Label\"." }
            }
          }
        },
        "managed": {
          "type": "object",
          "title": "Managed config",
          "description": "Written into hyprland.lua. Anything else goes in ~/.config/hypr/user/*.lua.",
          "x-showIf": { "mode": "managed" },
          "additionalProperties": false,
          "x-order": ["layout", "gapsIn", "gapsOut", "preserveSplit", "smartSplit", "smartResizing", "forceSplit", "splitRatio", "masterSize", "masterOrientation", "masterNewStatus", "columnWidth", "scrollDirection", "scrollFollowFocus", "snap", "matchAxiom", "borderSize", "rounding", "activeOpacity", "inactiveOpacity", "fullscreenOpacity", "dimInactive", "dimStrength", "dimSpecial", "windowBlur", "blurSize", "blurPasses", "blurXray", "blurIgnoreOpacity", "blurNewOptimizations", "blurNoise", "blurContrast", "blurVibrancy", "blurPopups", "blurSpecial", "shadow", "shadowRange", "shadowPower", "shadowThemed", "shadowColor", "animations", "animationPreset", "workspaceWraparound", "curves", "customAnimations", "kbLayout", "kbVariant", "kbOptions", "repeatRate", "repeatDelay", "numlockByDefault", "followMouse", "floatSwitchOverrideFocus", "accelProfile", "sensitivity", "leftHanded", "resizeOnBorder", "cursorTheme", "cursorSize", "cursorHideTimeout", "hideCursorOnKeyPress", "noWarps", "naturalScroll", "tapToClick", "disableWhileTyping", "touchpadScrollFactor", "hideLogo", "backgroundThemed", "backgroundColor", "focusOnActivate", "swallow", "swallowRegex", "closeSpecialOnEmpty", "hideSpecialOnWorkspaceChange", "workspaceBackAndForth", "mouseWakesScreen", "keyWakesScreen", "vrr", "scrollEventDelay", "xwaylandZeroScaling", "env", "autostart"],
          "properties": {
            "layout": { "type": "string", "title": "Layout", "x-group": "Layout", "enum": ["dwindle", "master", "scrolling"], "x-enumLabels": { "dwindle": "Dwindle (binary splits)", "master": "Master and stack", "scrolling": "Scrolling columns" }, "default": "dwindle", "x-hypr": "general.layout", "x-hyprAlways": true },
            "gapsIn": { "type": "integer", "title": "Gaps between windows", "x-group": "Layout", "default": 3, "minimum": 0, "maximum": 50, "x-hypr": "general.gaps_in", "x-hyprAlways": true },
            "gapsOut": { "type": "integer", "title": "Gaps to the edges", "x-group": "Layout", "default": 6, "minimum": 0, "maximum": 100, "x-hypr": "general.gaps_out", "x-hyprAlways": true },
            "preserveSplit": { "type": "boolean", "title": "Keep split direction", "x-group": "Layout", "default": true, "description": "Dwindle keeps a split's direction when the window is resized or moved.", "x-showIf": { "layout": "dwindle" }, "x-hypr": "dwindle.preserve_split", "x-hyprAlways": true },
            "smartSplit": { "type": "boolean", "title": "Split by cursor position", "x-group": "Layout", "default": false, "description": "Dwindle splits towards the side of the window the cursor is on.", "x-showIf": { "layout": "dwindle" }, "x-hypr": "dwindle.smart_split" },
            "smartResizing": { "type": "boolean", "title": "Resize by cursor position", "x-group": "Layout", "default": true, "description": "Resizing moves the edge nearest the cursor.", "x-showIf": { "layout": "dwindle" }, "x-hypr": "dwindle.smart_resizing" },
            "forceSplit": { "type": "string", "title": "New windows go", "x-group": "Layout", "enum": ["0", "1", "2"], "x-enumLabels": { "0": "Towards the cursor", "1": "Left or above", "2": "Right or below" }, "default": "0", "x-showIf": { "layout": "dwindle" }, "x-hypr": "dwindle.force_split", "x-hyprValue": "number" },
            "splitRatio": { "type": "integer", "title": "Split ratio", "x-unit": "%", "x-group": "Layout", "default": 100, "minimum": 10, "maximum": 190, "description": "The first window's share of a new split; 100 splits evenly.", "x-showIf": { "layout": "dwindle" }, "x-hypr": "dwindle.default_split_ratio", "x-hyprScale": 0.01 },
            "masterSize": { "type": "integer", "title": "Master size", "x-unit": "%", "x-group": "Layout", "default": 55, "minimum": 5, "maximum": 95, "x-showIf": { "layout": "master" }, "x-hypr": "master.mfact", "x-hyprScale": 0.01 },
            "masterOrientation": { "type": "string", "title": "Master side", "x-group": "Layout", "enum": ["left", "right", "top", "bottom", "center"], "default": "left", "x-showIf": { "layout": "master" }, "x-hypr": "master.orientation" },
            "masterNewStatus": { "type": "string", "title": "New windows become", "x-group": "Layout", "enum": ["slave", "master", "inherit"], "x-enumLabels": { "slave": "Part of the stack", "master": "The master", "inherit": "Like the focused window" }, "default": "slave", "x-showIf": { "layout": "master" }, "x-hypr": "master.new_status" },
            "columnWidth": { "type": "integer", "title": "Column width", "x-unit": "%", "x-group": "Layout", "default": 50, "minimum": 10, "maximum": 100, "x-showIf": { "layout": "scrolling" }, "x-hypr": "scrolling.column_width", "x-hyprScale": 0.01 },
            "scrollDirection": { "type": "string", "title": "Columns go", "x-group": "Layout", "enum": ["right", "left", "down", "up"], "default": "right", "x-showIf": { "layout": "scrolling" }, "x-hypr": "scrolling.direction" },
            "scrollFollowFocus": { "type": "boolean", "title": "Scroll to the focused window", "x-group": "Layout", "default": true, "x-showIf": { "layout": "scrolling" }, "x-hypr": "scrolling.follow_focus" },
            "snap": { "type": "boolean", "title": "Snap floating windows", "x-group": "Layout", "default": false, "description": "Floating windows snap to each other and the screen edges.", "x-hypr": "general.snap.enabled" },
            "matchAxiom": { "type": "boolean", "title": "Match axiom's look", "x-group": "Decoration", "default": true, "description": "Window corners and border width follow Look & Feel → Shape." },
            "borderSize": { "type": "integer", "title": "Border width", "x-group": "Decoration", "default": 2, "minimum": 0, "maximum": 20, "x-showIf": { "matchAxiom": false } },
            "rounding": { "type": "integer", "title": "Corner rounding", "x-group": "Decoration", "default": 8, "minimum": 0, "maximum": 50, "x-showIf": { "matchAxiom": false } },
            "activeOpacity": { "type": "integer", "title": "Focused window opacity", "x-unit": "%", "x-group": "Decoration", "default": 100, "minimum": 10, "maximum": 100, "x-hypr": "decoration.active_opacity", "x-hyprScale": 0.01 },
            "inactiveOpacity": { "type": "integer", "title": "Unfocused window opacity", "x-unit": "%", "x-group": "Decoration", "default": 100, "minimum": 10, "maximum": 100, "x-hypr": "decoration.inactive_opacity", "x-hyprScale": 0.01 },
            "fullscreenOpacity": { "type": "integer", "title": "Fullscreen opacity", "x-unit": "%", "x-group": "Decoration", "default": 100, "minimum": 10, "maximum": 100, "x-hypr": "decoration.fullscreen_opacity", "x-hyprScale": 0.01 },
            "dimInactive": { "type": "boolean", "title": "Dim unfocused windows", "x-group": "Decoration", "default": true, "x-hypr": "decoration.dim_inactive", "x-hyprAlways": true },
            "dimStrength": { "type": "integer", "title": "Dim strength", "x-unit": "%", "x-group": "Decoration", "default": 8, "minimum": 0, "maximum": 100, "x-showIf": { "dimInactive": true }, "x-hypr": "decoration.dim_strength", "x-hyprScale": 0.01, "x-hyprAlways": true },
            "dimSpecial": { "type": "integer", "title": "Dim behind special workspaces", "x-unit": "%", "x-group": "Decoration", "default": 20, "minimum": 0, "maximum": 100, "x-hypr": "decoration.dim_special", "x-hyprScale": 0.01 },
            "windowBlur": { "type": "boolean", "title": "Blur transparent windows", "x-group": "Blur", "default": true, "x-hypr": "decoration.blur.enabled", "x-hyprAlways": true },
            "blurSize": { "type": "integer", "title": "Blur size", "x-group": "Blur", "default": 4, "minimum": 1, "maximum": 20, "x-showIf": { "windowBlur": true }, "x-hypr": "decoration.blur.size", "x-hyprAlways": true },
            "blurPasses": { "type": "integer", "title": "Blur passes", "x-group": "Blur", "default": 3, "minimum": 1, "maximum": 6, "x-showIf": { "windowBlur": true }, "x-hypr": "decoration.blur.passes", "x-hyprAlways": true },
            "blurXray": { "type": "boolean", "title": "See-through blur", "x-group": "Blur", "default": true, "description": "Floating windows blur only the wallpaper, not the windows under them. Cheaper.", "x-showIf": { "windowBlur": true }, "x-hypr": "decoration.blur.xray", "x-hyprAlways": true },
            "blurIgnoreOpacity": { "type": "boolean", "title": "Full blur behind any transparency", "x-group": "Blur", "default": true, "x-showIf": { "windowBlur": true }, "x-hypr": "decoration.blur.ignore_opacity" },
            "blurNewOptimizations": { "type": "boolean", "title": "Blur optimizations", "x-group": "Blur", "default": true, "x-showIf": { "windowBlur": true }, "x-hypr": "decoration.blur.new_optimizations" },
            "blurNoise": { "type": "integer", "title": "Noise", "x-unit": "‰", "x-group": "Blur", "default": 12, "minimum": 0, "maximum": 1000, "x-showIf": { "windowBlur": true }, "x-hypr": "decoration.blur.noise", "x-hyprScale": 0.001 },
            "blurContrast": { "type": "integer", "title": "Contrast", "x-unit": "%", "x-group": "Blur", "default": 89, "minimum": 0, "maximum": 200, "x-showIf": { "windowBlur": true }, "x-hypr": "decoration.blur.contrast", "x-hyprScale": 0.01 },
            "blurVibrancy": { "type": "integer", "title": "Vibrancy", "x-unit": "%", "x-group": "Blur", "default": 17, "minimum": 0, "maximum": 100, "x-showIf": { "windowBlur": true }, "x-hypr": "decoration.blur.vibrancy", "x-hyprScale": 0.01 },
            "blurPopups": { "type": "boolean", "title": "Blur menus and popups", "x-group": "Blur", "default": false, "x-showIf": { "windowBlur": true }, "x-hypr": "decoration.blur.popups" },
            "blurSpecial": { "type": "boolean", "title": "Blur behind special workspaces", "x-group": "Blur", "default": false, "description": "Expensive: redraws the blur on every frame.", "x-showIf": { "windowBlur": true }, "x-hypr": "decoration.blur.special" },
            "shadow": { "type": "boolean", "title": "Window shadows", "x-group": "Shadows", "default": true, "x-hypr": "decoration.shadow.enabled" },
            "shadowRange": { "type": "integer", "title": "Shadow size", "x-group": "Shadows", "default": 4, "minimum": 0, "maximum": 100, "x-showIf": { "shadow": true }, "x-hypr": "decoration.shadow.range" },
            "shadowPower": { "type": "integer", "title": "Shadow falloff", "x-group": "Shadows", "default": 3, "minimum": 1, "maximum": 4, "description": "1 fades slowly, 4 ends sharply.", "x-showIf": { "shadow": true }, "x-hypr": "decoration.shadow.render_power" },
            "shadowThemed": { "type": "boolean", "title": "Shadow in a theme color", "x-group": "Shadows", "default": false, "x-showIf": { "shadow": true } },
            "shadowColor": { "type": "string", "title": "Shadow color", "x-group": "Shadows", "x-options": "colors", "default": "base00", "x-showIf": { "shadowThemed": true }, "x-hypr": "decoration.shadow.color", "x-hyprValue": "color", "x-hyprAlpha": "ee", "x-hyprIf": "shadowThemed" },
            "animations": { "type": "boolean", "title": "Animations", "x-group": "Animations", "default": true, "x-hypr": "animations.enabled", "x-hyprAlways": true },
            "animationPreset": { "type": "string", "title": "Style", "x-group": "Animations", "enum": ["hyprland", "axiom", "snappy", "bouncy", "custom"], "x-enumLabels": { "hyprland": "Hyprland's defaults", "axiom": "Follow axiom's motion", "snappy": "Snappy", "bouncy": "Bouncy", "custom": "Custom" }, "default": "axiom", "description": "Follow axiom's motion uses Look & Feel → Motion's speed, and turns Hyprland's animations off with axiom's.", "x-showIf": { "animations": true } },
            "workspaceWraparound": { "type": "boolean", "title": "Wrap workspace animations", "x-group": "Animations", "default": false, "description": "Going from the last workspace to the first slides forward, not back.", "x-showIf": { "animations": true }, "x-hypr": "animations.workspace_wraparound" },
            "curves": {
              "type": "array",
              "title": "Curves",
              "x-group": "Animations",
              "default": [],
              "description": "Named bezier curves for the custom animations.",
              "x-showIf": { "animationPreset": "custom" },
              "items": {
                "type": "object",
                "additionalProperties": false,
                "x-order": ["name", "points"],
                "properties": {
                  "name": { "type": "string", "title": "Name", "default": "" },
                  "points": { "type": "string", "title": "Points", "default": "0.25, 0.1, 0.25, 1", "description": "x1, y1, x2, y2, as in CSS cubic-bezier()." }
                }
              }
            },
            "customAnimations": {
              "type": "array",
              "title": "Animations",
              "x-group": "Animations",
              "default": [],
              "description": "One entry per animation to change; a parent (windows) covers its children (windowsIn, windowsOut, windowsMove).",
              "x-showIf": { "animationPreset": "custom" },
              "items": {
                "type": "object",
                "additionalProperties": false,
                "x-order": ["leaf", "enabled", "durationMs", "curve", "style"],
                "properties": {
                  "leaf": { "type": "string", "title": "Animation", "enum": ["global", "windows", "windowsIn", "windowsOut", "windowsMove", "layers", "layersIn", "layersOut", "fade", "fadeIn", "fadeOut", "fadeSwitch", "fadeShadow", "fadeDim", "border", "borderangle", "workspaces", "workspacesIn", "workspacesOut", "specialWorkspace", "specialWorkspaceIn", "specialWorkspaceOut", "zoomFactor", "monitorAdded"], "default": "windows" },
                  "enabled": { "type": "boolean", "title": "On", "default": true },
                  "durationMs": { "type": "integer", "title": "Duration", "x-unit": "ms", "default": 300, "minimum": 10, "maximum": 5000 },
                  "curve": { "type": "string", "title": "Curve", "default": "default", "description": "A curve's name from above, or default." },
                  "style": { "type": "string", "title": "Style", "default": "", "description": "Optional: popin 80%, slide, slidevert, slidefade 20%, fade, gnomed, loop, once." }
                }
              }
            },
            "kbLayout": { "type": "string", "title": "Keyboard layout", "x-group": "Keyboard", "default": "us", "description": "XKB layouts, comma-separated (e.g. us,de).", "x-hypr": "input.kb_layout", "x-hyprAlways": true },
            "kbVariant": { "type": "string", "title": "Layout variant", "x-group": "Keyboard", "default": "", "description": "XKB variants, one per layout (e.g. colemak).", "x-hypr": "input.kb_variant" },
            "kbOptions": { "type": "string", "title": "XKB options", "x-group": "Keyboard", "default": "", "description": "e.g. caps:escape,grp:alt_shift_toggle.", "x-hypr": "input.kb_options" },
            "repeatRate": { "type": "integer", "title": "Key repeat rate", "x-group": "Keyboard", "default": 50, "minimum": 1, "maximum": 100, "x-hypr": "input.repeat_rate", "x-hyprAlways": true },
            "repeatDelay": { "type": "integer", "title": "Key repeat delay", "x-group": "Keyboard", "default": 300, "minimum": 100, "maximum": 2000, "x-hypr": "input.repeat_delay", "x-hyprAlways": true },
            "numlockByDefault": { "type": "boolean", "title": "Num Lock on at start", "x-group": "Keyboard", "default": true, "x-hypr": "input.numlock_by_default", "x-hyprAlways": true },
            "followMouse": { "type": "boolean", "title": "Focus follows mouse", "x-group": "Mouse & cursor", "default": true, "x-hypr": "input.follow_mouse", "x-hyprValue": "int", "x-hyprAlways": true },
            "floatSwitchOverrideFocus": { "type": "string", "title": "Crossing tiled/floating", "x-group": "Mouse & cursor", "enum": ["0", "1", "2"], "x-enumLabels": { "0": "Leaves focus alone", "1": "Moves focus", "2": "Moves focus, also floating to floating" }, "default": "1", "description": "What moving the cursor between tiled and floating windows does.", "x-showIf": { "followMouse": true }, "x-hypr": "input.float_switch_override_focus", "x-hyprValue": "number" },
            "accelProfile": { "type": "string", "title": "Pointer acceleration", "x-group": "Mouse & cursor", "enum": ["default", "flat", "adaptive"], "x-enumLabels": { "default": "Device default", "flat": "Flat (none)", "adaptive": "Adaptive" }, "default": "default", "x-hypr": "input.accel_profile", "x-hyprValue": "emptyIsDefault", "x-hyprAlways": true },
            "sensitivity": { "type": "integer", "title": "Pointer speed", "x-group": "Mouse & cursor", "default": 0, "minimum": -100, "maximum": 100, "description": "-100 to 100; 0 leaves it as the device reports.", "x-hypr": "input.sensitivity", "x-hyprScale": 0.01 },
            "leftHanded": { "type": "boolean", "title": "Left-handed buttons", "x-group": "Mouse & cursor", "default": false, "x-hypr": "input.left_handed" },
            "resizeOnBorder": { "type": "boolean", "title": "Resize by dragging borders", "x-group": "Mouse & cursor", "default": true, "x-hypr": "general.resize_on_border", "x-hyprAlways": true },
            "cursorTheme": { "type": "string", "title": "Cursor theme", "x-group": "Mouse & cursor", "x-options": "cursorThemes", "x-enumLabels": { "": "None" }, "default": "", "description": "An installed cursor theme. None leaves the cursor alone." },
            "cursorSize": { "type": "integer", "title": "Cursor size", "x-group": "Mouse & cursor", "default": 24, "minimum": 8, "maximum": 128 },
            "cursorHideTimeout": { "type": "integer", "title": "Hide the cursor after", "x-unit": "s", "x-group": "Mouse & cursor", "default": 0, "minimum": 0, "maximum": 600, "description": "0 never hides it.", "x-hypr": "cursor.inactive_timeout" },
            "hideCursorOnKeyPress": { "type": "boolean", "title": "Hide the cursor while typing", "x-group": "Mouse & cursor", "default": false, "x-hypr": "cursor.hide_on_key_press" },
            "noWarps": { "type": "boolean", "title": "Never move the cursor", "x-group": "Mouse & cursor", "default": false, "description": "Hyprland won't move the cursor to a window it focuses.", "x-hypr": "cursor.no_warps" },
            "naturalScroll": { "type": "boolean", "title": "Natural scrolling", "x-group": "Touchpad", "default": false, "x-hypr": "input.touchpad.natural_scroll", "x-hyprAlways": true },
            "tapToClick": { "type": "boolean", "title": "Tap to click", "x-group": "Touchpad", "default": true, "x-hypr": "input.touchpad.tap_to_click" },
            "disableWhileTyping": { "type": "boolean", "title": "Off while typing", "x-group": "Touchpad", "default": true, "x-hypr": "input.touchpad.disable_while_typing" },
            "touchpadScrollFactor": { "type": "integer", "title": "Scroll speed", "x-unit": "%", "x-group": "Touchpad", "default": 100, "minimum": 10, "maximum": 500, "x-hypr": "input.touchpad.scroll_factor", "x-hyprScale": 0.01 },
            "hideLogo": { "type": "boolean", "title": "Hide Hyprland's logo and splash", "x-group": "Behaviour", "default": true, "x-hypr": ["misc.disable_hyprland_logo", "misc.disable_splash_rendering"], "x-hyprAlways": true },
            "backgroundThemed": { "type": "boolean", "title": "Background in a theme color", "x-group": "Behaviour", "default": false, "description": "What shows where there's no wallpaper." },
            "backgroundColor": { "type": "string", "title": "Background color", "x-group": "Behaviour", "x-options": "colors", "default": "base00", "x-showIf": { "backgroundThemed": true }, "x-hypr": "misc.background_color", "x-hyprValue": "color", "x-hyprIf": "backgroundThemed" },
            "focusOnActivate": { "type": "boolean", "title": "Focus windows that ask for it", "x-group": "Behaviour", "default": true, "description": "e.g. a browser window opened from a link.", "x-hypr": "misc.focus_on_activate", "x-hyprAlways": true },
            "swallow": { "type": "boolean", "title": "Terminals swallow what they open", "x-group": "Behaviour", "default": true, "description": "A GUI app started from a terminal takes the terminal's place until it closes.", "x-hypr": "misc.enable_swallow", "x-hyprAlways": true },
            "swallowRegex": { "type": "string", "title": "Swallowing terminals", "x-group": "Behaviour", "default": "^(kitty|foot|Alacritty|com\\.mitchellh\\.ghostty|org\\.wezfurlong\\.wezterm)$", "description": "A regex of terminal window classes.", "x-showIf": { "swallow": true }, "x-hypr": "misc.swallow_regex", "x-hyprIf": "swallow" },
            "closeSpecialOnEmpty": { "type": "boolean", "title": "Close empty special workspaces", "x-group": "Behaviour", "default": true, "x-hypr": "misc.close_special_on_empty" },
            "hideSpecialOnWorkspaceChange": { "type": "boolean", "title": "Hide special workspaces on switching", "x-group": "Behaviour", "default": false, "x-hypr": "binds.hide_special_on_workspace_change" },
            "workspaceBackAndForth": { "type": "boolean", "title": "Workspace back and forth", "x-group": "Behaviour", "default": false, "description": "Going to the current workspace goes back to the previous one.", "x-hypr": "binds.workspace_back_and_forth" },
            "mouseWakesScreen": { "type": "boolean", "title": "Mouse wakes the screen", "x-group": "Behaviour", "default": true, "x-hypr": "misc.mouse_move_enables_dpms", "x-hyprAlways": true },
            "keyWakesScreen": { "type": "boolean", "title": "Keys wake the screen", "x-group": "Behaviour", "default": false, "x-hypr": "misc.key_press_enables_dpms" },
            "vrr": { "type": "string", "title": "Variable refresh rate", "x-group": "Behaviour", "enum": ["0", "1", "2"], "x-enumLabels": { "0": "Off", "1": "On", "2": "Fullscreen only" }, "default": "0", "x-hypr": "misc.vrr", "x-hyprValue": "number" },
            "scrollEventDelay": { "type": "integer", "title": "Scroll bind delay", "x-unit": "ms", "x-group": "Behaviour", "default": 300, "minimum": 0, "maximum": 2000, "description": "How long a bound scroll ignores further scrolling.", "x-hypr": "binds.scroll_event_delay" },
            "xwaylandZeroScaling": { "type": "boolean", "title": "Unscaled X11 apps", "x-group": "Behaviour", "default": true, "description": "X11 apps draw at native resolution on scaled monitors, instead of blurry.", "x-hypr": "xwayland.force_zero_scaling", "x-hyprAlways": true },
            "env": {
              "type": "array",
              "title": "Environment",
              "x-group": "Startup",
              "default": [{ "name": "QT_QPA_PLATFORM", "value": "wayland;xcb" }],
              "description": "Variables set before anything starts. Changes apply to apps started afterwards.",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "x-order": ["name", "value"],
                "properties": {
                  "name": { "type": "string", "title": "Name", "default": "" },
                  "value": { "type": "string", "title": "Value", "default": "" }
                }
              }
            },
            "autostart": {
              "type": "array",
              "title": "Autostart",
              "x-group": "Startup",
              "default": [
                { "command": "dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP", "enabled": true },
                { "command": "systemctl --user start hyprpolkitagent", "enabled": true },
                { "command": "hypridle", "enabled": true }
              ],
              "description": "Commands run once when Hyprland starts, after axiom.",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "x-order": ["command", "enabled"],
                "properties": {
                  "command": { "type": "string", "title": "Command", "default": "" },
                  "enabled": { "type": "boolean", "title": "On", "default": true }
                }
              }
            }
          }
        }
      }
    },

    "Appearance": {
      "type": "object",
      "x-category": "Look & Feel",
      "x-links": ["Themes"],
      "title": "Appearance",
      "additionalProperties": false,
      "properties": {
        "theme": {
          "type": "string",
          "default": "gruvbox-dark",
          "description": "Active theme (set on the overlay's Themes page).",
          "x-settings": false
        },
        "wallpaper": {
          "type": "string",
          "default": "",
          "description": "The primary monitor's wallpaper file URL, used by the lockscreen (set on the overlay's Themes page).",
          "x-settings": false
        },
        "wallpapers": {
          "type": "object",
          "default": {},
          "description": "Monitor name → wallpaper file URL (set on the overlay's Themes page). A monitor with no entry shows `wallpaper`.",
          "additionalProperties": { "type": "string" },
          "x-settings": false
        },
        "wallpaperFolder": {
          "type": "string",
          "title": "Wallpaper folder",
          "default": "~/Pictures/wallpapers",
          "pattern": "\\S",
          "description": "Folder the wallpaper pickers list images from (jpg, png, bmp). A leading ~ is your home folder."
        },
        "font": {
          "type": "object",
          "title": "Font",
          "additionalProperties": false,
          "properties": {
            "family": {
              "type": "string",
              "title": "Family",
              "default": "monospace",
              "description": "Font used throughout the shell."
            },
            "size": {
              "type": "integer",
              "title": "Size",
              "default": 14,
              "minimum": 8,
              "maximum": 32,
              "x-unit": "px",
              "description": "Base font size (px). Larger text is derived from it."
            }
          }
        },
        "shape": {
          "type": "object",
          "title": "Shape",
          "additionalProperties": false,
          "properties": {
            "radius": {
              "type": "integer",
              "title": "Corner radius",
              "default": 6,
              "minimum": 0,
              "maximum": 50,
              "x-unit": "px",
              "description": "Roundness of every corner: widgets, popouts, cards and the screen border (px)."
            },
            "borderWidth": {
              "type": "integer",
              "title": "Border width",
              "default": 1,
              "minimum": 0,
              "maximum": 10,
              "x-unit": "px",
              "description": "Thickness of every outline (px)."
            },
            "screenBorder": {
              "type": "boolean",
              "title": "Screen border",
              "default": true,
              "description": "Draw a frame around the screen edges."
            },
            "screenMargin": {
              "type": "integer",
              "title": "Screen margin",
              "default": 8,
              "minimum": 0,
              "maximum": 100,
              "x-unit": "px",
              "description": "Width of the screen frame, and the gap kept from the screen edges (px)."
            }
          }
        },
        "motion": {
          "type": "object",
          "title": "Motion",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean",
              "title": "Animations",
              "default": true,
              "description": "Turn every animation in the shell on or off."
            },
            "speed": {
              "type": "integer",
              "title": "Animation speed",
              "x-unit": "%",
              "default": 100,
              "minimum": 25,
              "maximum": 400,
              "description": "Speed of every animation. 100 is normal, 200 is twice as fast, 50 is half speed."
            }
          }
        }
      }
    },

    "Widget": {
      "type": "object",
      "x-category": "Look & Feel",
      "title": "Widgets",
      "description": "Shared sizing for widgets and form controls. Bar widgets are as thick as their bar, minus twice its widget inset.",
      "additionalProperties": false,
      "properties": {
        "height": {
          "type": "integer",
          "title": "Control height",
          "default": 30,
          "minimum": 16,
          "maximum": 128,
          "x-unit": "px",
          "description": "Height of buttons, fields and other controls outside the bar (px). Bar widgets follow their bar's thickness."
        },
        "padding": {
          "type": "integer",
          "title": "Padding",
          "default": 10,
          "minimum": 0,
          "maximum": 32,
          "x-unit": "px",
          "description": "Inner padding of widgets and panels (px)."
        },
        "spacing": {
          "type": "integer",
          "title": "Spacing",
          "default": 4,
          "minimum": 0,
          "maximum": 32,
          "x-unit": "px",
          "description": "Gap between items inside widgets and panels (px)."
        }
      }
    },

    "Popouts": {
      "type": "object",
      "x-category": "Bar & Popouts",
      "x-links": ["BarEditor"],
      "title": "Popouts",
      "description": "Behaviour shared by bar popouts and screen-edge popouts (side panel, OSD).",
      "additionalProperties": false,
      "properties": {
        "openDelay": {
          "type": "integer",
          "title": "Open delay",
          "x-unit": "ms",
          "default": 10,
          "minimum": 0,
          "maximum": 2000,
          "description": "How long to hover a bar widget or screen edge before its popout opens."
        },
        "dismissDelay": {
          "type": "integer",
          "title": "Dismiss delay",
          "x-unit": "ms",
          "default": 250,
          "minimum": 0,
          "maximum": 5000,
          "description": "How long after the mouse leaves a popout before it closes."
        },
        "edgeTriggerSize": {
          "type": "integer",
          "title": "Edge trigger size",
          "x-unit": "px",
          "default": 5,
          "minimum": 1,
          "maximum": 50,
          "description": "Thickness of the hover strip at the screen edge that opens edge popouts."
        }
      }
    },

    "Chat": {
      "type": "object",
      "x-category": "Chat",
      "x-card": "ChatProviders",
      "title": "Chat",
      "description": "The Chat overlay module, and the launcher's @ prefix. API keys are never stored here: set them on the card above (they go to your keyring, or $XDG_STATE_HOME/axiom/secrets.json without one), or in each provider's environment variable.",
      "additionalProperties": false,
      "properties": {
        "defaultProvider": {
          "type": "string",
          "title": "Default provider",
          "x-group": "Conversations",
          "default": "anthropic",
          "x-options": "chatProviders",
          "description": "Used by presets that don't name a provider of their own."
        },
        "defaultPreset": {
          "type": "string",
          "title": "Default preset",
          "x-group": "Conversations",
          "default": "Default",
          "x-options": "chatPresets",
          "description": "What a new conversation starts with."
        },
        "maxTokens": { "type": "integer", "title": "Longest reply", "x-unit": "tokens", "x-group": "Conversations", "default": 16000, "minimum": 256, "maximum": 128000, "description": "A reply that reaches this is cut off, with a note saying so." },
        "keepConversations": { "type": "integer", "title": "Conversations kept", "x-group": "Conversations", "default": 50, "minimum": 0, "maximum": 500, "description": "Older ones are deleted. 0 keeps none: conversations last until the shell restarts." },
        "showThinking": { "type": "boolean", "title": "Show thinking", "x-group": "Display", "default": true, "description": "Ask models that reason for a summary of it, shown folded above the reply." },
        "renderMarkdown": { "type": "boolean", "title": "Format replies", "x-group": "Display", "default": true, "description": "Draw Markdown (headings, lists, tables, code blocks) instead of the raw text." },
        "sendKey": {
          "type": "string",
          "title": "Send with",
          "x-group": "Display",
          "enum": ["enter", "ctrlEnter"],
          "x-enumLabels": { "enter": "Enter (Shift+Enter for a new line)", "ctrlEnter": "Ctrl+Enter (Enter for a new line)" },
          "default": "enter"
        },
        "providers": {
          "type": "array",
          "title": "Providers",
          "description": "Where chats are sent. The OpenAI kind also covers Ollama, LM Studio, llama.cpp, OpenRouter, Groq and anything else with an OpenAI-style API: set its address.",
          "default": [
            { "id": "anthropic", "name": "Anthropic", "kind": "anthropic", "baseUrl": "https://api.anthropic.com/v1", "auth": "key", "keyEnv": "ANTHROPIC_API_KEY", "models": ["claude-opus-5", "claude-sonnet-5", "claude-haiku-4-5", "claude-fable-5-1", "claude-opus-5-5"], "defaultModel": "claude-opus-5", "fallbacks": true },
            { "id": "openai", "name": "OpenAI", "kind": "openai", "baseUrl": "https://api.openai.com/v1", "auth": "key", "keyEnv": "OPENAI_API_KEY", "models": ["gpt-5", "gpt-5-mini"], "defaultModel": "gpt-5", "fallbacks": false },
            { "id": "gemini", "name": "Gemini", "kind": "gemini", "baseUrl": "https://generativelanguage.googleapis.com/v1beta", "auth": "key", "keyEnv": "GEMINI_API_KEY", "models": ["gemini-2.5-pro", "gemini-2.5-flash"], "defaultModel": "gemini-2.5-flash", "fallbacks": false },
            { "id": "ollama", "name": "Ollama", "kind": "openai", "baseUrl": "http://localhost:11434/v1", "auth": "none", "keyEnv": "", "models": [], "defaultModel": "", "fallbacks": false }
          ],
          "items": {
            "type": "object",
            "additionalProperties": false,
            "x-order": ["name", "id", "kind", "baseUrl", "auth", "keyEnv", "models", "defaultModel", "fallbacks"],
            "properties": {
              "id": { "type": "string", "title": "ID", "default": "", "description": "Names the provider's key in the keyring and presets' provider field. Lowercase letters, digits and dashes." },
              "name": { "type": "string", "title": "Name", "default": "" },
              "kind": {
                "type": "string",
                "title": "API",
                "enum": ["anthropic", "openai", "gemini"],
                "x-enumLabels": { "anthropic": "Anthropic Messages", "openai": "OpenAI-compatible", "gemini": "Google Gemini" },
                "default": "openai"
              },
              "baseUrl": { "type": "string", "title": "Address", "default": "", "description": "The API's base URL, up to and including its version (e.g. http://localhost:11434/v1)." },
              "auth": {
                "type": "string",
                "title": "Key",
                "enum": ["key", "none"],
                "x-enumLabels": { "key": "Needs an API key", "none": "No key (local server)" },
                "default": "key"
              },
              "keyEnv": { "type": "string", "title": "Key variable", "default": "", "description": "An environment variable that holds the key, if you keep it there. It wins over a stored key.", "x-showIf": { "auth": "key" } },
              "models": { "type": "array", "title": "Models", "default": [], "items": { "type": "string" }, "description": "Model ids to choose from, comma-separated. Fetch models on the card above fills this in." },
              "defaultModel": { "type": "string", "title": "Default model", "default": "", "description": "Empty uses the first of the models." },
              "fallbacks": { "type": "boolean", "title": "Refusal fallback", "default": true, "description": "When the model declines a request, the API retries it on another model within the same reply.", "x-showIf": { "kind": "anthropic" } }
            }
          }
        },
        "presets": {
          "type": "array",
          "title": "Presets",
          "description": "Saved setups to start a conversation with: a system prompt, and optionally a provider, model and effort of their own.",
          "default": [
            { "name": "Default", "icon": "chat", "systemPrompt": "You are a helpful assistant built into the user's Linux desktop. Answer concisely, and use Markdown when it helps.", "provider": "", "model": "", "effort": "default" },
            { "name": "Coder", "icon": "code", "systemPrompt": "You are an expert programmer. Lead with working code, then explain briefly. Prefer small, idiomatic changes.", "provider": "", "model": "", "effort": "high" }
          ],
          "items": {
            "type": "object",
            "additionalProperties": false,
            "x-order": ["name", "icon", "systemPrompt", "provider", "model", "effort"],
            "properties": {
              "name": { "type": "string", "title": "Name", "default": "" },
              "icon": { "type": "string", "title": "Icon", "default": "chat", "description": "A Material Symbols name (fonts.google.com/icons)." },
              "systemPrompt": { "type": "string", "title": "System prompt", "default": "", "x-multiline": true },
              "provider": { "type": "string", "title": "Provider", "default": "", "description": "A provider ID. Empty uses the default provider." },
              "model": { "type": "string", "title": "Model", "default": "", "description": "Empty uses the provider's default model." },
              "effort": {
                "type": "string",
                "title": "Effort",
                "enum": ["default", "low", "medium", "high", "xhigh", "max"],
                "x-enumLabels": { "default": "Model default", "low": "Low", "medium": "Medium", "high": "High", "xhigh": "Extra high", "max": "Max" },
                "default": "default",
                "description": "How hard a reasoning model thinks. Models that don't reason ignore it."
              }
            }
          }
        }
      }
    },

    "OSD": {
      "type": "object",
      "x-category": "Overlay & OSD",
      "title": "OSD",
      "description": "On-screen display of volume, microphone and brightness bars, shown when one of them changes.",
      "additionalProperties": false,
      "x-order": ["enabled", "monitors", "edge", "position", "orientation", "alongEdge", "openOnHover", "timeout", "bars"],
      "properties": {
        "enabled": {
          "type": "boolean",
          "title": "Enabled",
          "default": true,
          "description": "Show the OSD when one of its bars changes."
        },
        "monitors": {
          "type": "string",
          "title": "Monitors",
          "enum": ["general", "primaryBar", "focused", "all"],
          "x-enumLabels": { "general": "As in General", "primaryBar": "Primary bar's monitor", "focused": "Focused monitor", "all": "Every monitor" },
          "default": "general",
          "description": "Where the OSD appears. As in General follows General > Monitors; the primary bar's monitor is the first bar's; the focused monitor is the one with your active workspace (and mouse); every monitor shows it on all of them at once."
        },
        "edge": {
          "type": "string",
          "title": "Screen edge",
          "default": "Bottom",
          "enum": ["Top", "Bottom", "Left", "Right"],
          "description": "Screen edge the OSD slides out of."
        },
        "position": {
          "type": "integer",
          "title": "Position along edge",
          "x-unit": "%",
          "default": 50,
          "minimum": 0,
          "maximum": 100,
          "description": "Where along the edge it opens: 0 is the start (left/top), 100 the end."
        },
        "orientation": {
          "type": "string",
          "title": "Bar orientation",
          "default": "Vertical",
          "enum": ["Vertical", "Horizontal"],
          "description": "Vertical bars sit side by side; horizontal bars stack as rows."
        },
        "alongEdge": {
          "type": "boolean",
          "title": "Line up along the edge",
          "default": false,
          "description": "Place the bars in one line along the screen edge instead of stacking them by orientation. Bars that run along the edge then sit end to end, so the OSD is longer but sticks out less."
        },
        "openOnHover": {
          "type": "boolean",
          "title": "Open on hover",
          "default": false,
          "description": "Also open the OSD when the mouse touches the screen edge where it appears."
        },
        "timeout": {
          "type": "integer",
          "title": "Timeout",
          "x-unit": "ms",
          "default": 1000,
          "minimum": 250,
          "maximum": 10000,
          "description": "How long the OSD stays open after the last change."
        },
        "bars": {
          "type": "array",
          "title": "Bars",
          "x-group": "OSD bars",
          "description": "One bar per entry, in order. System volume is the output device's volume, Other apps is the first playing stream no App bar matches, Microphone is the input device's volume, and Brightness is the monitor's own (a laptop panel through brightnessctl, an external monitor over DDC/CI through ddcutil; left out on a monitor that has neither).",
          "default": [
            { "type": "other", "icon": "music_note", "showOsd": true },
            { "type": "master", "icon": "", "showOsd": true }
          ],
          "items": {
            "type": "object",
            "additionalProperties": false,
            "x-order": ["type", "app", "icon", "showOsd"],
            "properties": {
              "type": {
                "type": "string",
                "title": "Shows",
                "enum": ["master", "other", "microphone", "brightness", "app"],
                "x-enumLabels": { "master": "System volume", "other": "Other apps", "microphone": "Microphone", "brightness": "Brightness", "app": "An app" },
                "default": "app"
              },
              "app": { "type": "string", "title": "App", "default": "", "x-showIf": { "type": "app" }, "description": "Matched as a substring of the stream's binary or name." },
              "icon": { "type": "string", "title": "Icon", "default": "", "description": "Icon shown on the bar: a Material Symbols name (fonts.google.com/icons), or text. Empty follows the level for system volume, microphone and brightness." },
              "showOsd": { "type": "boolean", "title": "Opens the OSD", "default": true, "description": "A change on this bar opens the OSD (it always keeps an open one open)." }
            }
          }
        }
      }
    },

    "ThemeIntegrations": {
      "type": "object",
      "x-category": "Look & Feel",
      "x-links": ["Themes"],
      "title": "Theme integrations",
      "description": "Re-theme external apps whenever the shell theme changes. Each writes its own axiom file and never edits your configs: its description gives the one line to add to the app's config.",
      "additionalProperties": false,
      "properties": {
        "gtk": { "type": "boolean", "title": "GTK", "default": false, "description": "Writes axiom.css in ~/.config/gtk-3.0 and gtk-4.0: add '@import url(\"axiom.css\");' to each gtk.css (created with it when missing), and follows light/dark. GTK3 apps need the adw-gtk3 theme (pacman -S adw-gtk-theme)." },
        "qt": { "type": "boolean", "title": "Qt (qt5ct / qt6ct)", "default": false, "description": "Writes an \"axiom\" color scheme: pick it in qt5ct/qt6ct (Palette: Custom)." },
        "kitty": { "type": "boolean", "title": "Kitty", "default": false, "description": "Writes ~/.config/kitty/axiom.conf: add 'include axiom.conf' to kitty.conf. Reloads kitty when allow_remote_control is on." },
        "alacritty": { "type": "boolean", "title": "Alacritty", "default": false, "description": "Writes ~/.config/alacritty/axiom.toml: add it to 'import' under [general] in alacritty.toml. Alacritty reloads it by itself." },
        "foot": { "type": "boolean", "title": "Foot", "default": false, "description": "Writes ~/.config/foot/axiom.ini: add 'include=~/.config/foot/axiom.ini' to foot.ini. Applies to new windows." },
        "wezterm": { "type": "boolean", "title": "WezTerm", "default": false, "description": "Writes the \"axiom\" color scheme: set 'config.color_scheme = \"axiom\"' in wezterm.lua." },
        "ghostty": { "type": "boolean", "title": "Ghostty", "default": false, "description": "Writes the \"axiom\" theme: set 'theme = axiom' in the Ghostty config. Ghostty 1.2+ reloads it." },
        "nvim": { "type": "boolean", "title": "Neovim", "default": false, "description": "Writes a palette for the axiom_theme module in your Neovim config, and reloads running instances." },
        "helix": { "type": "boolean", "title": "Helix", "default": false, "description": "Writes the \"axiom\" theme: set 'theme = \"axiom\"' in config.toml. Helix 23.10+ reloads it." },
        "vscode": { "type": "boolean", "title": "VS Code / VSCodium", "default": false, "description": "Installs a local \"Axiom\" color theme extension: pick it under Preferences: Color Theme." },
        "k9s": { "type": "boolean", "title": "k9s", "default": false, "description": "Writes the \"axiom\" skin: set 'k9s.ui.skin: axiom' in the k9s config." },
        "cava": { "type": "boolean", "title": "Cava", "default": false, "description": "Writes ~/.config/cava/colors/axiom.conf: include it in the Cava config (created with it when missing)." },
        "btop": { "type": "boolean", "title": "btop", "default": false, "description": "Writes the \"axiom\" theme: set 'color_theme = \"axiom\"' in btop.conf." },
        "fzf": { "type": "boolean", "title": "fzf", "default": false, "description": "Writes --color options: export FZF_DEFAULT_OPTS_FILE=$XDG_STATE_HOME/axiom/fzf-colors (fzf 0.49+)." },
        "lazygit": { "type": "boolean", "title": "lazygit", "default": false, "description": "Writes ~/.config/lazygit/axiom.yml: append it to LG_CONFIG_FILE, after your config.yml." },
        "bat": { "type": "boolean", "title": "bat / delta", "default": false, "description": "Writes the \"axiom\" syntax theme and rebuilds bat's cache: use '--theme=axiom' (delta: 'syntax-theme = axiom')." },
        "yazi": { "type": "boolean", "title": "Yazi", "default": false, "description": "Writes the \"axiom\" flavor: set it as 'dark' and 'light' under [flavor] in theme.toml (Yazi 25.5+)." }
      }
    },

    "Icons": {
      "type": "object",
      "x-category": "Bar & Popouts",
      "title": "Icons",
      "additionalProperties": false,
      "properties": {
        "overrides": {
          "type": "object",
          "default": {},
          "description": "App name → icon path/URL, for apps whose tray or window icon doesn't resolve.",
          "additionalProperties": { "type": "string" },
          "x-settings": false
        }
      }
    },

    "Overlay": {
      "type": "object",
      "x-category": "Overlay & OSD",
      "x-links": ["OverlayEditor"],
      "title": "Overlay",
      "additionalProperties": false,
      "properties": {
        "monitors": {
          "type": "string",
          "title": "Monitors",
          "enum": ["general", "primaryBar", "focused", "all"],
          "x-enumLabels": { "general": "As in General", "primaryBar": "Primary bar's monitor", "focused": "Focused monitor", "all": "Every monitor" },
          "default": "general",
          "description": "Where the overlay opens. As in General follows General > Monitors; the primary bar's monitor is the first bar's; the focused monitor is the one with your active workspace (and mouse); every monitor shows it on all of them at once."
        },
        "size": {
          "type": "integer",
          "title": "Overlay size",
          "x-unit": "%",
          "default": 100,
          "minimum": 50,
          "maximum": 150,
          "description": "Size of the overlay's cards, relative to what fits the screen. Pages that would still overflow are shrunk to fit."
        },
        "closeOnEscape": {
          "type": "boolean",
          "title": "Close on Escape",
          "default": false,
          "description": "Close the overlay when Escape is pressed (unless something in it, like a popup, uses the key first)."
        },
        "closeOnOutsideClick": {
          "type": "boolean",
          "title": "Close on outside click",
          "default": false,
          "description": "Close the overlay when you click outside it: on a bar, the screen border or another monitor."
        },
        "views": {
          "type": "array",
          "x-settings": false,
          "description": "Pages shown in the overlay, in order. A Custom view lays out columns side by side; a Cells column stacks cell layouts, whose slots each hold one module.",
          "default": [
            { "type": "Custom", "name": "Home", "columns": [
              { "cells": [
                { "layout": "Tall", "slots": {
                  "main": { "type": "ClockCalendar" }
                } }
              ] },
              { "cells": [
                { "layout": "Horiz1x1", "slots": {
                  "top": { "type": "QuickActions" },
                  "bottom": { "type": "QuickActions", "properties": { "actions": ["lock", "suspend", "hibernate", "logout", "reboot", "poweroff"] } }
                } },
                { "layout": "Single", "slots": {
                  "main": { "type": "SystemGraphs" }
                } }
              ] },
              { "cells": [
                { "layout": "Wide", "slots": {
                  "main": { "type": "NowPlaying" }
                } },
                { "layout": "Single", "slots": {
                  "main": { "type": "Notifications" }
                } },
                { "layout": "Single", "slots": {
                  "main": { "type": "VolumeDials" }
                } }
              ] }
            ] },
            { "type": "Settings" },
            { "type": "Keybinds" },
            { "type": "BarEditor" },
            { "type": "Themes" },
            { "type": "EdgeMenuEditor" }
          ],
          "items": { "$ref": "#/definitions/OverlayView" }
        }
      }
    },

    "Workspaces": {
      "type": "object",
      "x-category": "Desktop",
      "title": "Workspaces",
      "description": "How workspaces are laid out, for the bar's workspace widget, the workspace overview and the Workspaces module. Point your workspace keybinds at `qs -c axiom ipc call workspaces <left|right|up|down|go N|nth N>` (and the move… versions) so they follow it too.",
      "additionalProperties": false,
      "properties": {
        "layout": {
          "type": "string",
          "title": "Layout",
          "enum": ["standard", "grid"],
          "x-enumLabels": { "standard": "Standard (1 to N)", "grid": "Grid per monitor" },
          "default": "standard",
          "description": "Standard: workspaces 1 to N, shared by every monitor. Grid: each monitor has its own columns × rows workspaces, numbered in Hyprland's monitor order."
        },
        "count": {
          "type": "integer",
          "title": "Workspaces",
          "default": 10,
          "minimum": 1,
          "maximum": 30,
          "x-showIf": { "layout": "standard" }
        },
        "columns": {
          "type": "integer",
          "title": "Columns",
          "default": 5,
          "minimum": 1,
          "maximum": 10,
          "x-showIf": { "layout": "grid" }
        },
        "rows": {
          "type": "integer",
          "title": "Rows",
          "default": 5,
          "minimum": 1,
          "maximum": 10,
          "x-showIf": { "layout": "grid" }
        },
        "wrap": {
          "type": "boolean",
          "title": "Wrap around",
          "default": false,
          "description": "Stepping past the last workspace (of a row or column in a grid) goes back to the first."
        },
        "animate": {
          "type": "boolean",
          "title": "Directional slides",
          "default": true,
          "description": "Slide between workspaces whatever Hyprland's own workspace animation style is: sideways in the standard layout; in a grid, sideways between columns and vertically between rows, going by row first, then column."
        }
      }
    },

    "WorkspaceOverlay": {
      "type": "object",
      "x-category": "Overlay & OSD",
      "title": "Workspace overview",
      "description": "The full-screen workspace board (laid out as in Workspaces): drag windows between workspaces, right-drag to resize, middle-click to close.",
      "additionalProperties": false,
      "properties": {
        "background": {
          "type": "string",
          "title": "Workspace background",
          "enum": ["wallpaper", "color"],
          "x-enumLabels": { "wallpaper": "Monitor's wallpaper", "color": "Theme color" },
          "default": "wallpaper",
          "description": "What each workspace shows behind its windows."
        },
        "color": {
          "type": "string",
          "title": "Background color",
          "x-options": "colors",
          "default": "base01",
          "description": "The workspaces' color.",
          "x-showIf": { "background": "color" }
        },
        "dimInactive": {
          "type": "integer",
          "title": "Dim other workspaces",
          "x-unit": "%",
          "default": 50,
          "minimum": 0,
          "maximum": 90,
          "description": "How much darker workspaces other than the active one are drawn."
        },
        "size": {
          "type": "integer",
          "title": "Size",
          "x-unit": "%",
          "default": 85,
          "minimum": 40,
          "maximum": 100,
          "description": "How much of the screen the grid may take."
        },
        "backdrop": {
          "type": "integer",
          "title": "Backdrop darkness",
          "x-unit": "%",
          "default": 70,
          "minimum": 0,
          "maximum": 100,
          "description": "How dark the screen behind the grid goes."
        },
        "showNumbers": {
          "type": "boolean",
          "title": "Workspace numbers",
          "default": true,
          "description": "Show each workspace's number in its corner."
        },
        "showTitles": {
          "type": "boolean",
          "title": "Window titles",
          "default": true,
          "description": "Show a window's title while it's hovered."
        },
        "showHint": {
          "type": "boolean",
          "title": "Controls hint",
          "default": true,
          "description": "Show the mouse controls under the grid."
        }
      }
    },

    "SelfUpdate": {
      "type": "object",
      "x-category": "Updates",
      "x-card": "SelfUpdateStatus",
      "title": "axiom updates",
      "description": "Updates come from release tags on the repository axiom was cloned from. An update only fast-forwards: a clone with changed files or commits of its own is never touched.",
      "additionalProperties": false,
      "properties": {
        "mode": {
          "type": "string",
          "title": "Updates",
          "enum": ["auto", "notify", "off"],
          "x-enumLabels": { "auto": "Update automatically", "notify": "Notify me", "off": "Off" },
          "default": "notify",
          "description": "Checked when the shell starts and once a day. Update automatically installs a new release and notifies you; Notify me sends a notification that opens this page. Off never checks."
        }
      }
    },

    "Bars": {
      "type": "array",
      "description": "Bars, in priority order: the first is the primary bar. Edited in the overlay's bar editor.",
      "x-settings": false,
      "default": [
        {
          "id": "primary",
          "location": "Top",
          "extent": 34,
          "inset": 4,
          "background": "pills",
          "lockCenter": true,
          "widgets": {
            "left": [
              { "type": "Button", "properties": { "action": "overlay", "icon": "menu", "tooltip": "Overlay", "backgroundColor": "base0E" } },
              { "type": "Workspaces" },
              { "type": "Window" }
            ],
            "center": [
              { "type": "Time", "properties": { "showSeconds": false } }
            ],
            "right": [
              { "type": "SystemTray" },
              { "type": "Volume" },
              { "type": "Network" },
              { "type": "Bluetooth", "properties": { "hideWhenOff": true } },
              { "type": "Battery" },
              { "type": "Notifications" },
              { "type": "Button", "properties": { "action": "powerMenu", "tooltip": "Power", "backgroundColor": "base08" } }
            ]
          }
        }
      ],
      "items": { "$ref": "#/definitions/Bar" }
    },

    "EdgeMenus": {
      "type": "array",
      "description": "Menus that pop out of a screen edge, filled with overlay modules, edited on the overlay's Edge menu editor page. Opened by a bar Button (Edge menu action), their edge trigger, or `qs -c axiom ipc call edgeMenu toggle <id>`.",
      "x-settings": false,
      "default": [],
      "items": { "$ref": "#/definitions/EdgeMenu" }
    }
  },

  "definitions": {
    "EdgeMenu": {
      "type": "object",
      "required": ["id"],
      "additionalProperties": false,
      "x-order": ["name", "id", "enabled", "monitor", "mode", "edge", "position", "edgeDistance", "cardSize", "extraDepth", "frame", "margin", "padding", "backgroundColor", "borderColor", "openOnHover", "openDelay", "triggerSize", "triggerLength", "closeOnLeave", "closeDelay", "closeOnOutsideClick"],
      "properties": {
        "id": { "type": "string", "title": "Id", "description": "What bar buttons and IPC calls open this menu by." },
        "name": { "type": "string", "title": "Name", "default": "" },
        "enabled": { "type": "boolean", "title": "Enabled", "default": true },
        "monitor": {
          "type": "string",
          "title": "Monitor",
          "default": "",
          "description": "Monitor this menu is shown on. Empty uses the primary monitor.",
          "x-options": "screens"
        },
        "edge": { "type": "string", "title": "Edge", "default": "Left", "enum": ["Top", "Bottom", "Left", "Right"] },
        "mode": {
          "type": "string",
          "title": "Mode",
          "default": "floating",
          "enum": ["floating", "integrated"],
          "x-enumLabels": { "floating": "Floating", "integrated": "Integrated" },
          "description": "Floating opens over your windows. Integrated opens outside the screen border and bars, pushing them and your windows inwards."
        },
        "position": {
          "type": "integer",
          "title": "Position along edge",
          "default": 50,
          "minimum": 0,
          "maximum": 100,
          "x-unit": "%",
          "description": "Where along the edge the menu sits: 0 is the start (left/top), 100 the end."
        },
        "cardSize": {
          "type": "integer",
          "title": "Card size",
          "default": 320,
          "minimum": 160,
          "maximum": 600,
          "x-unit": "px",
          "description": "Size of one card. Cells are laid out as on overlay pages."
        },
        "extraDepth": {
          "type": "integer",
          "title": "Extra depth",
          "default": 0,
          "minimum": 0,
          "maximum": 1000,
          "x-unit": "px",
          "x-control": "slider",
          "description": "Room added across the edge: width on a left or right edge, height on a top or bottom one. Every module grows into it."
        },
        "edgeDistance": {
          "type": "integer",
          "title": "Distance from edge",
          "default": 0,
          "minimum": 0,
          "maximum": 200,
          "x-unit": "px",
          "description": "Gap between the menu and the screen edge. Above 0 the menu is a separate box instead of growing out of the edge.",
          "x-showIf": { "mode": "floating" }
        },
        "frame": {
          "type": "boolean",
          "title": "Frame",
          "default": true,
          "description": "Draw a rounded box along the whole edge, with the modules inside it.",
          "x-showIf": { "mode": "integrated" }
        },
        "margin": {
          "type": "integer",
          "title": "Frame margin",
          "default": 8,
          "minimum": 0,
          "maximum": 64,
          "x-unit": "px",
          "description": "Gap between the frame and the edges of the menu's strip.",
          "x-showIf": { "mode": "integrated" }
        },
        "padding": {
          "type": "integer",
          "title": "Padding",
          "default": 12,
          "minimum": 0,
          "maximum": 64,
          "x-unit": "px",
          "description": "Space between the menu's box and its modules."
        },
        "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base00" },
        "borderColor": { "type": "string", "title": "Border", "x-options": "colors", "default": "base05" },
        "openOnHover": { "type": "boolean", "title": "Open on hover", "default": false, "description": "Open when the pointer rests on the screen edge." },
        "openDelay": {
          "type": "integer",
          "title": "Open delay",
          "default": 10,
          "minimum": 0,
          "maximum": 2000,
          "x-unit": "ms",
          "description": "How long the pointer rests on the edge before the menu opens.",
          "x-showIf": { "openOnHover": true }
        },
        "triggerSize": {
          "type": "integer",
          "title": "Trigger size",
          "default": 5,
          "minimum": 1,
          "maximum": 50,
          "x-unit": "px",
          "description": "How far in from the screen edge the hover strip reaches.",
          "x-showIf": { "openOnHover": true }
        },
        "triggerLength": {
          "type": "integer",
          "title": "Trigger length",
          "default": 0,
          "minimum": 0,
          "maximum": 4000,
          "x-unit": "px",
          "description": "Length of the hover strip along the edge, centred on the menu. 0 matches the menu's own length.",
          "x-showIf": { "openOnHover": true }
        },
        "closeOnLeave": { "type": "boolean", "title": "Close when the pointer leaves", "default": true, "description": "A Pin module keeps the menu open while pinned." },
        "closeDelay": {
          "type": "integer",
          "title": "Close delay",
          "default": 250,
          "minimum": 0,
          "maximum": 5000,
          "x-unit": "ms",
          "description": "How long after the pointer leaves the menu closes.",
          "x-showIf": { "closeOnLeave": true }
        },
        "closeOnOutsideClick": { "type": "boolean", "title": "Close on outside click", "default": false, "x-showIf": { "mode": "floating" } },
        "columns": {
          "type": "array",
          "title": "Columns",
          "default": [],
          "x-settings": false,
          "description": "Placed side by side, like a Custom overlay page's.",
          "items": { "$ref": "#/definitions/OverlayColumn" }
        }
      }
    },

    "Bar": {
      "type": "object",
      "required": ["id"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "title": "Id" },
        "enabled": { "type": "boolean", "title": "Enabled", "default": true },
        "monitor": {
          "type": "string",
          "title": "Monitor",
          "default": "",
          "description": "Monitor this bar is shown on. Empty uses the first screen.",
          "x-options": "screens"
        },
        "location": {
          "type": "string",
          "title": "Location",
          "default": "Top",
          "enum": ["Top", "Bottom", "Left", "Right"]
        },
        "extent": {
          "type": "integer",
          "title": "Thickness",
          "default": 30,
          "minimum": 10,
          "maximum": 200,
          "x-unit": "px",
          "description": "Thickness of the bar (px). A pill bar is as thick as its pills: its widgets plus the pill padding."
        },
        "inset": {
          "type": "integer",
          "title": "Widget inset",
          "default": 0,
          "minimum": 0,
          "maximum": 50,
          "x-unit": "px",
          "description": "Gap between the widgets and the bar's edges (px). Widgets are the bar's thickness minus twice this, whatever the background."
        },
        "background": {
          "type": "string",
          "title": "Background",
          "default": "solid",
          "enum": ["solid", "transparent", "pills"],
          "x-enumLabels": { "solid": "Solid", "transparent": "Transparent", "pills": "Pill per group" },
          "description": "Transparent and pill bars sit inside the screen border; pills grow out of it."
        },
        "pillPadding": {
          "type": "integer",
          "title": "Pill padding",
          "default": 4,
          "minimum": 0,
          "maximum": 20,
          "x-showIf": { "background": "pills" },
          "x-unit": "px",
          "description": "Extra space around a pill's widgets (px). They sit the screen margin plus this from the screen edge, and as far from the pill's other edges."
        },
        "pillMerge": {
          "type": "integer",
          "title": "Merge distance",
          "default": 12,
          "minimum": 0,
          "maximum": 200,
          "x-showIf": { "background": "pills" },
          "x-unit": "px",
          "description": "Groups closer than this share one pill (px)."
        },
        "spacing": {
          "type": "integer",
          "title": "Spacing",
          "default": 4,
          "minimum": 0,
          "maximum": 50,
          "x-unit": "px",
          "description": "Gap between widgets (px)."
        },
        "lockCenter": {
          "type": "boolean",
          "title": "Lock center",
          "default": false,
          "description": "Keep the center section dead center on the bar. The other sections shrink or hide widgets on their own side instead of pushing it."
        },
        "reserveSpace": {
          "type": "boolean",
          "title": "Reserve space",
          "default": true,
          "description": "Keep windows from tiling underneath the bar."
        },
        "widgets": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "left": { "type": "array", "default": [], "items": { "$ref": "#/definitions/BarWidget" } },
            "leftCenter": { "type": "array", "default": [], "items": { "$ref": "#/definitions/BarWidget" } },
            "center": { "type": "array", "default": [], "items": { "$ref": "#/definitions/BarWidget" } },
            "rightCenter": { "type": "array", "default": [], "items": { "$ref": "#/definitions/BarWidget" } },
            "right": { "type": "array", "default": [], "items": { "$ref": "#/definitions/BarWidget" } }
          }
        }
      }
    },

    "OverlayView": {
      "type": "object",
      "required": ["type"],
      "oneOf": [
        { "$ref": "#/definitions/CustomOverlayView" },
        { "$ref": "#/definitions/KeybindsOverlayView" },
        { "$ref": "#/definitions/BarEditorOverlayView" },
        { "$ref": "#/definitions/SettingsOverlayView" },
        { "$ref": "#/definitions/ThemesOverlayView" },
        { "$ref": "#/definitions/EdgeMenuEditorOverlayView" }
      ]
    },
    "CustomOverlayView": {
      "type": "object",
      "x-icon": "dashboard",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Custom", "description": "Custom" },
        "visible": { "type": "boolean" },
        "name": { "type": "string", "title": "Name", "default": "" },
        "columns": { "type": "array", "default": [], "items": { "$ref": "#/definitions/OverlayColumn" } }
      }
    },
    "KeybindsOverlayView": {
      "type": "object",
      "x-icon": "keyboard",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Keybinds", "description": "Keybinds" },
        "visible": { "type": "boolean" }
      }
    },
    "SettingsOverlayView": {
      "type": "object",
      "x-icon": "settings",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Settings", "description": "Settings" },
        "visible": { "type": "boolean" }
      }
    },
    "BarEditorOverlayView": {
      "type": "object",
      "x-icon": "toolbar",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "BarEditor", "description": "Bar Editor" },
        "visible": { "type": "boolean" }
      }
    },
    "ThemesOverlayView": {
      "type": "object",
      "x-icon": "palette",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Themes", "description": "Themes" },
        "visible": { "type": "boolean" }
      }
    },
    "EdgeMenuEditorOverlayView": {
      "type": "object",
      "x-icon": "dock_to_right",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "EdgeMenuEditor", "description": "Edge Menu Editor" },
        "visible": { "type": "boolean" }
      }
    },

    "OverlayColumn": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "cells": { "type": "array", "default": [], "items": { "$ref": "#/definitions/OverlayCell" } }
      }
    },

    "OverlayCell": {
      "type": "object",
      "required": ["layout"],
      "additionalProperties": false,
      "description": "One cell split into slots, defined in OverlayConfig.layouts. A column flows its cells left to right, wrapping at its widest cell, so two Singles after a Wide sit side by side under it (an empty Single leaves a gap). Cells are one card, except Tall (two cards high), Wide (two cards wide), Large (two by two cards), HalfWide (half a card high) and HalfTall (half a card wide). A cell with fill grows into free room: the rest of its row, its row's height, and spare height in its column (shared evenly with other fill cells); in an edge menu, any fill cell makes the menu take its whole edge. Slots per layout: Single/Tall/Wide/Large/HalfWide/HalfTall main; Grid2x2 topLeft/topRight/bottomLeft/bottomRight; Vert1x1 left/right; Vert1x2 left/topRight/bottomRight; Vert2x1 topLeft/bottomLeft/right; Horiz1x1 top/bottom; Horiz1x2 top/bottomLeft/bottomRight; Horiz2x1 topLeft/topRight/bottom.",
      "properties": {
        "layout": { "type": "string", "title": "Layout", "default": "Single", "enum": ["Single", "Tall", "Wide", "Large", "HalfWide", "HalfTall", "Grid2x2", "Vert1x1", "Vert1x2", "Vert2x1", "Horiz1x1", "Horiz1x2", "Horiz2x1"] },
        "fill": { "type": "boolean", "title": "Fill space", "default": false, "description": "Grow into free room, shared evenly with other cells that fill." },
        "slots": {
          "type": "object",
          "default": {},
          "additionalProperties": false,
          "properties": {
            "main": { "$ref": "#/definitions/OverlayModule" },
            "topLeft": { "$ref": "#/definitions/OverlayModule" },
            "topRight": { "$ref": "#/definitions/OverlayModule" },
            "bottomLeft": { "$ref": "#/definitions/OverlayModule" },
            "bottomRight": { "$ref": "#/definitions/OverlayModule" },
            "top": { "$ref": "#/definitions/OverlayModule" },
            "bottom": { "$ref": "#/definitions/OverlayModule" },
            "left": { "$ref": "#/definitions/OverlayModule" },
            "right": { "$ref": "#/definitions/OverlayModule" }
          }
        }
      }
    },

    "OverlayModule": {
      "type": "object",
      "required": ["type"],
      "oneOf": [
        { "$ref": "#/definitions/ThemeEditorOverlayModule" },
        { "$ref": "#/definitions/NowPlayingOverlayModule" },
        { "$ref": "#/definitions/AudioMixerOverlayModule" },
        { "$ref": "#/definitions/VolumeDialsOverlayModule" },
        { "$ref": "#/definitions/SystemGraphsOverlayModule" },
        { "$ref": "#/definitions/DisksOverlayModule" },
        { "$ref": "#/definitions/TopProcessesOverlayModule" },
        { "$ref": "#/definitions/UpdatesOverlayModule" },
        { "$ref": "#/definitions/QuickActionsOverlayModule" },
        { "$ref": "#/definitions/WorkspacesMapOverlayModule" },
        { "$ref": "#/definitions/NotificationsOverlayModule" },
        { "$ref": "#/definitions/BluetoothDevicesOverlayModule" },
        { "$ref": "#/definitions/WifiNetworksOverlayModule" },
        { "$ref": "#/definitions/ScreenshotOverlayModule" },
        { "$ref": "#/definitions/ClockCalendarOverlayModule" },
        { "$ref": "#/definitions/WeatherOverlayModule" },
        { "$ref": "#/definitions/BatteryOverlayModule" },
        { "$ref": "#/definitions/NetworkOverlayModule" },
        { "$ref": "#/definitions/FavouritesOverlayModule" },
        { "$ref": "#/definitions/ChatOverlayModule" },
        { "$ref": "#/definitions/NotesOverlayModule" },
        { "$ref": "#/definitions/WallpaperPickerOverlayModule" },
        { "$ref": "#/definitions/ColorSwatchOverlayModule" },
        { "$ref": "#/definitions/PinOverlayModule" }
      ]
    },
    "PinOverlayModule": {
      "type": "object",
      "x-icon": "push_pin",
      "x-hosts": ["edgeMenu"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Pin", "description": "Pin (keeps an edge menu open)" },
        "properties": { "type": "object", "additionalProperties": false, "properties": {} }
      }
    },
    "ThemeEditorOverlayModule": {
      "type": "object",
      "x-icon": "palette",
      "x-shapes": ["vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "ThemeEditor", "description": "Theme editor (use a Tall cell)" }
      }
    },
    "NowPlayingOverlayModule": {
      "type": "object",
      "x-icon": "music_note",
      "x-shapes": ["square", "horizontal"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "NowPlaying", "description": "Now playing" }
      }
    },
    "AudioMixerOverlayModule": {
      "type": "object",
      "x-icon": "tune",
      "x-shapes": ["vertical", "square"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "AudioMixer", "description": "Audio mixer" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "mode": {"type": "string", "title": "Start on", "default": "output", "enum": ["output", "input"]}
          }
        }
      }
    },
    "VolumeDialsOverlayModule": {
      "type": "object",
      "x-icon": "volume_up",
      "x-shapes": ["square", "horizontal", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "VolumeDials", "description": "Volume dials" }
      }
    },
    "SystemGraphsOverlayModule": {
      "type": "object",
      "x-icon": "show_chart",
      "x-shapes": ["square", "horizontal", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "SystemGraphs", "description": "System graphs" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "metrics": {"type": "array", "title": "Metrics", "default": ["cpu", "mem"], "items": {"type": "string", "enum": ["cpu", "mem", "gpu", "cpuTemp", "net"]}}
          }
        }
      }
    },
    "DisksOverlayModule": {
      "type": "object",
      "x-icon": "hard_drive",
      "x-shapes": ["horizontal", "square", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Disks", "description": "Disk usage" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "paths": {"type": "array", "title": "Mount points", "default": ["/"], "items": {"type": "string"}, "description": "Comma-separated, e.g. /, /home"}
          }
        }
      }
    },
    "TopProcessesOverlayModule": {
      "type": "object",
      "x-icon": "format_list_bulleted",
      "x-shapes": ["vertical", "square"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "TopProcesses", "description": "Top processes" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "count": {"type": "integer", "title": "Processes", "default": 8, "minimum": 1, "maximum": 30},
            "sortBy": {"type": "string", "title": "Sort by", "default": "cpu", "enum": ["cpu", "mem"]}
          }
        }
      }
    },
    "UpdatesOverlayModule": {
      "type": "object",
      "x-icon": "upload",
      "x-shapes": ["square", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Updates", "description": "Package updates" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "intervalMinutes": {"type": "integer", "title": "Check interval", "x-unit": "min", "default": 60, "minimum": 5, "maximum": 1440},
            "includeAur": {"type": "boolean", "title": "Include AUR", "default": false},
            "aurHelper": {"type": "string", "title": "AUR helper", "default": "paru", "enum": ["paru", "yay"], "x-showIf": {"includeAur": true}}
          }
        }
      }
    },
    "QuickActionsOverlayModule": {
      "type": "object",
      "x-icon": "toggle_on",
      "x-shapes": ["square", "horizontal", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "QuickActions", "description": "Quick actions" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "actions": {"type": "array", "title": "Actions", "default": ["wifi", "bluetooth", "caffeine", "dnd", "darkMode"], "description": "Toggles, session actions and pin, in any mix. Night light and power saver only show when hyprsunset/wlsunset or powerprofilesctl is installed, and pin only in an edge menu. Log out, reboot and power off ask for a second click.", "items": {"type": "string", "enum": ["wifi", "bluetooth", "caffeine", "dnd", "darkMode", "nightLight", "powerSaver", "lock", "suspend", "hibernate", "logout", "reboot", "poweroff", "pin"]}},
            "labels": {"type": "string", "title": "Names", "default": "auto", "enum": ["auto", "show", "hide"], "x-enumLabels": {"auto": "Auto", "show": "Show", "hide": "Hide"}, "description": "Auto shows the names only when every tile has room for its whole name."}
          }
        }
      }
    },
    "WorkspacesMapOverlayModule": {
      "type": "object",
      "x-icon": "grid_view",
      "x-shapes": ["horizontal", "square", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "WorkspacesMap", "description": "Workspaces" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showEmpty": {"type": "boolean", "title": "Show empty workspaces", "default": true}
          }
        }
      }
    },
    "NotificationsOverlayModule": {
      "type": "object",
      "x-icon": "notifications",
      "x-shapes": ["vertical", "square"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Notifications", "description": "Notifications" }
      }
    },
    "WifiNetworksOverlayModule": {
      "type": "object",
      "x-icon": "wifi",
      "x-shapes": ["vertical", "square"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "WifiNetworks", "description": "Wi-Fi networks" }
      }
    },
    "BluetoothDevicesOverlayModule": {
      "type": "object",
      "x-icon": "bluetooth",
      "x-shapes": ["vertical", "square"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "BluetoothDevices", "description": "Bluetooth devices" }
      }
    },
    "ScreenshotOverlayModule": {
      "type": "object",
      "x-icon": "camera",
      "x-shapes": ["square", "horizontal"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Screenshot", "description": "Screenshot" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "directory": {"type": "string", "title": "Save to", "default": "~/Pictures/Screenshots"}
          }
        }
      }
    },
    "ClockCalendarOverlayModule": {
      "type": "object",
      "x-icon": "calendar_clock",
      "x-shapes": ["square", "vertical", "horizontal"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "ClockCalendar", "description": "Clock & calendar" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "use24h": {"type": "boolean", "title": "24-hour clock", "default": true},
            "showSeconds": {"type": "boolean", "title": "Show seconds", "default": false}
          }
        }
      }
    },
    "WeatherOverlayModule": {
      "type": "object",
      "x-icon": "partly_cloudy_day",
      "x-shapes": ["square", "horizontal", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Weather", "description": "Weather" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "location": {"type": "string", "title": "Location", "default": "", "description": "City name; empty uses your IP's location."},
            "latitude": {"type": "string", "title": "Latitude", "default": "", "description": "Exact coordinates; with longitude, overrides the location lookup."},
            "longitude": {"type": "string", "title": "Longitude", "default": ""},
            "units": {"type": "string", "title": "Units", "default": "celsius", "enum": ["celsius", "fahrenheit"]}
          }
        }
      }
    },
    "BatteryOverlayModule": {
      "type": "object",
      "x-icon": "battery_full",
      "x-shapes": ["square", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Battery", "description": "Battery" }
      }
    },
    "NetworkOverlayModule": {
      "type": "object",
      "x-icon": "wifi",
      "x-shapes": ["square", "horizontal", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Network", "description": "Network" }
      }
    },
    "FavouritesOverlayModule": {
      "type": "object",
      "x-icon": "star",
      "x-shapes": ["square", "horizontal", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Favourites", "description": "Favourite apps" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "apps": {"type": "array", "title": "Apps", "default": [], "items": {"type": "string"}, "description": "Desktop entry ids, comma-separated (e.g. firefox, kitty). Empty shows recently launched apps."},
            "showLabels": {"type": "boolean", "title": "Show names", "default": true}
          }
        }
      }
    },
    "ChatOverlayModule": {
      "type": "object",
      "x-icon": "smart_toy",
      "x-shapes": ["vertical", "square", "horizontal"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Chat", "description": "AI chat" }
      }
    },
    "NotesOverlayModule": {
      "type": "object",
      "x-icon": "sticky_note_2",
      "x-shapes": ["vertical", "square", "horizontal"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Notes", "description": "Notes" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "name": {"type": "string", "title": "Note name", "default": "", "description": "Notes with the same name share their text."}
          }
        }
      }
    },
    "WallpaperPickerOverlayModule": {
      "type": "object",
      "x-icon": "photo_library",
      "x-shapes": ["horizontal", "square", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "WallpaperPicker", "description": "Wallpapers (a Tall cell adds a monitor picker and a preview)" }
      }
    },
    "ColorSwatchOverlayModule": {
      "type": "object",
      "x-icon": "palette",
      "x-shapes": ["square", "horizontal", "vertical"],
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "ColorSwatch", "description": "Theme color swatch" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "color": { "type": "string", "title": "Color", "default": "base00", "x-options": "colors" },
            "label": { "type": "string", "title": "Label", "default": "", "description": "Secondary label, e.g. the semantic role." }
          }
        }
      }
    },

    "WidgetLayout": {
      "type": "object",
      "title": "Layout",
      "description": "Overrides for how much room a bar widget takes. Anything left out uses the widget's own sizing.",
      "additionalProperties": false,
      "properties": {
        "size": { "type": "integer", "title": "Size", "minimum": 0, "maximum": 2000, "x-unit": "px", "description": "Length along the bar (px). Fixed and elastic widgets use it as their size / maximum." },
        "minSize": { "type": "integer", "title": "Minimum size", "minimum": 0, "maximum": 2000, "x-unit": "px", "description": "Smallest length the widget may shrink to when the bar is crowded (px)." },
        "priority": { "type": "integer", "title": "Priority", "minimum": -100, "maximum": 100, "description": "When a section can't fit its widgets, lower priorities are hidden first." }
      }
    },

    "BarWidget": {
      "type": "object",
      "required": ["type"],
      "oneOf": [
        { "$ref": "#/definitions/WindowWidget" },
        { "$ref": "#/definitions/MediaWidget" },
        { "$ref": "#/definitions/WorkspacesWidget" },
        { "$ref": "#/definitions/TimeWidget" },
        { "$ref": "#/definitions/TailscaleWidget" },
        { "$ref": "#/definitions/NetworkWidget" },
        { "$ref": "#/definitions/SystemTrayWidget" },
        { "$ref": "#/definitions/NotificationsWidget" },
        { "$ref": "#/definitions/ButtonWidget" },
        { "$ref": "#/definitions/BatteryWidget" },
        { "$ref": "#/definitions/SystemStatsWidget" },
        { "$ref": "#/definitions/KeyboardLayoutWidget" },
        { "$ref": "#/definitions/IdleInhibitorWidget" },
        { "$ref": "#/definitions/PrivacyWidget" },
        { "$ref": "#/definitions/UpdatesWidget" },
        { "$ref": "#/definitions/WeatherWidget" },
        { "$ref": "#/definitions/SeparatorWidget" },
        { "$ref": "#/definitions/VolumeWidget" },
        { "$ref": "#/definitions/MicrophoneWidget" },
        { "$ref": "#/definitions/BluetoothWidget" }
      ]
    },
    "WindowWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Window", "description": "Window Title" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "icon": { "type": "string", "title": "Icon", "default": "", "description": "Icon shown before the title: a Material Symbols name (fonts.google.com/icons), or text. Empty shows none." },
            "emptyText": { "type": "string", "title": "Empty text", "default": "—", "description": "Shown when no window is focused." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0D" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "MediaWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Media", "description": "Media Player" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showArtist": { "type": "boolean", "title": "Show artist", "default": true },
            "artistLength": { "type": "integer", "title": "Artist length", "default": 10, "minimum": 0, "maximum": 100, "description": "Longest artist name shown before it is shortened (characters)." },
            "idleText": { "type": "string", "title": "Idle text", "default": "No player", "description": "Shown when nothing is playing." },
            "showPopout": { "type": "boolean", "title": "Popout", "default": true, "description": "Open the media controls when hovered." },
            "playingColor": { "type": "string", "title": "Playing background", "x-options": "colors", "default": "base0B" },
            "pausedColor": { "type": "string", "title": "Paused background", "x-options": "colors", "default": "base02" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "WorkspacesWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Workspaces", "description": "Workspace Switcher" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showEmpty": { "type": "boolean", "title": "Show empty workspaces", "default": true, "x-showIf": { "/Workspaces.layout": "standard" }, "description": "Off: only workspaces with windows, plus the active one." },
            "monitorOnly": { "type": "boolean", "title": "Only this monitor", "default": false, "x-showIf": { "/Workspaces.layout": "standard" }, "description": "Hide workspaces that are open on another monitor." },
            "labels": { "type": "string", "title": "Labels", "enum": ["numbers", "dots"], "x-enumLabels": { "numbers": "Numbers", "dots": "Dots" }, "default": "numbers", "description": "In a grid, numbers count through the monitor's grid, on the bar and in the popout." },
            "showAppIcons": { "type": "boolean", "title": "App icons", "default": false, "description": "Show the largest window's icon on occupied workspaces (in a grid, on the bar and in the popout)." },
            "wideActive": { "type": "boolean", "title": "Wide active", "default": true, "x-showIf": { "/Workspaces.layout": "standard" }, "description": "Make the active workspace twice as long." },
            "showActiveIcon": { "type": "boolean", "title": "Active icon", "default": true, "x-showIf": { "/Workspaces.layout": "grid" }, "description": "Show an arrow on the active workspace, pointing to where the shown row (or column) is in the grid." },
            "showPopout": { "type": "boolean", "title": "Popout", "default": true, "x-showIf": { "/Workspaces.layout": "grid" }, "description": "Open the whole grid when hovered." },
            "clickToSwitch": { "type": "boolean", "title": "Click to switch", "default": true, "description": "Click a workspace (on the bar or in the grid popout) to go to it." },
            "scrollToSwitch": { "type": "boolean", "title": "Scroll to switch", "default": true, "description": "Scroll over the widget to step through the workspaces (along the shown row or column in a grid)." },
            "activeColor": { "type": "string", "title": "Active", "x-options": "colors", "default": "base0E" },
            "occupiedColor": { "type": "string", "title": "Occupied", "x-options": "colors", "default": "base04" },
            "emptyColor": { "type": "string", "title": "Empty", "x-options": "colors", "default": "base01" },
            "textColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00", "description": "Numbers, and the grid's active arrow. The colors also apply to the grid popout." }
          }
        }
      }
    },
    "TimeWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Time", "description": "Clock" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "use24Hour": { "type": "boolean", "title": "24-hour", "default": false },
            "showSeconds": { "type": "boolean", "title": "Seconds", "default": true },
            "showDate": { "type": "boolean", "title": "Date", "default": true },
            "timeFormat": { "type": "string", "title": "Time format", "default": "", "description": "Custom Qt time format (e.g. HH:mm). Empty uses the language's format." },
            "dateFormat": { "type": "string", "title": "Date format", "default": "", "description": "Custom Qt date format (e.g. ddd d MMM). Empty uses the language's format." },
            "showCalendar": { "type": "boolean", "title": "Calendar popout", "default": true, "description": "Open the calendar when hovered." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0C" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "TailscaleWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Tailscale", "description": "Tailscale Status" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showLabel": { "type": "boolean", "title": "Show label", "default": true },
            "label": { "type": "string", "title": "Label", "default": "", "description": "Text shown while connected. Empty shows the tailnet name." },
            "interval": { "type": "integer", "title": "Poll interval", "x-unit": "ms", "default": 2000, "minimum": 250, "maximum": 60000 },
            "connectedColor": { "type": "string", "title": "Connected background", "x-options": "colors", "default": "base0A" },
            "disconnectedColor": { "type": "string", "title": "Disconnected background", "x-options": "colors", "default": "base02" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "NetworkWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Network", "description": "Network Status" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showName": { "type": "boolean", "title": "Show device", "default": false, "description": "Show the connected device name next to the icon." },
            "showPopout": { "type": "boolean", "title": "Popout", "default": true, "description": "Open the Wi-Fi menu when hovered." },
            "middleCommand": { "type": "string", "title": "Middle-click command", "default": "", "description": "Shell command run on middle click (e.g. nm-connection-editor). Empty does nothing." },
            "backgroundColor": { "type": "string", "title": "Connected background", "x-options": "colors", "default": "base09" },
            "disconnectedColor": { "type": "string", "title": "Disconnected background", "x-options": "colors", "default": "base08" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "SystemTrayWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "SystemTray", "description": "System Tray" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "iconSize": { "type": "integer", "title": "Icon size", "default": 18, "minimum": 8, "maximum": 64, "x-unit": "px", "description": "Size of tray icons (px)." },
            "showPassive": { "type": "boolean", "title": "Show passive", "default": true, "description": "Also show items that report themselves as passive (idle)." }
          }
        }
      }
    },
    "NotificationsWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Notifications", "description": "Notification Center" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showCount": { "type": "boolean", "title": "Show count", "default": true, "description": "Badge with the number of notifications." },
            "showPopout": { "type": "boolean", "title": "Popout", "default": true, "description": "Open the notification list when hovered." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0D" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" },
            "dndColor": { "type": "string", "title": "Do-not-disturb icon", "x-options": "colors", "default": "base04" },
            "badgeColor": { "type": "string", "title": "Badge", "x-options": "colors", "default": "base08" }
          }
        }
      }
    },
    "ButtonWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Button", "description": "Button / Command" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "icon": { "type": "string", "title": "Icon", "default": "apps", "description": "Icon shown on the button: a Material Symbols name (fonts.google.com/icons), or text." },
            "label": { "type": "string", "title": "Label", "default": "", "description": "Text next to the icon. Replaced by the label command's output when that is set." },
            "tooltip": { "type": "string", "title": "Tooltip", "default": "", "description": "Shown when hovering the button." },
            "action": { "type": "string", "title": "Click action", "default": "powerMenu", "description": "What a left click does.", "enum": ["powerMenu", "appLauncher", "overlay", "workspaceOverlay", "edgeMenu", "lock", "command", "none"] },
            "menu": { "type": "string", "title": "Edge menu", "default": "", "description": "The edge menu the button opens and closes.", "x-options": "edgeMenus", "x-showIf": { "action": "edgeMenu" } },
            "command": { "type": "string", "title": "Command", "default": "", "description": "Shell command run on left click.", "x-showIf": { "action": "command" } },
            "rightCommand": { "type": "string", "title": "Right-click command", "default": "", "description": "Shell command run on right click. Empty does nothing." },
            "middleCommand": { "type": "string", "title": "Middle-click command", "default": "", "description": "Shell command run on middle click. Empty does nothing." },
            "hoverActivate": { "type": "boolean", "title": "Activate on hover", "default": false, "description": "Run the click action after hovering for the popout open delay. Doesn't close a launcher, overlay, power menu or edge menu that is already open.", "x-showIf": { "action": { "not": "none" } } },
            "labelCommand": { "type": "string", "title": "Label command", "default": "", "description": "Shell command whose first line of output is shown as the label, re-run on an interval." },
            "labelInterval": { "type": "integer", "title": "Label interval", "x-unit": "ms", "default": 5000, "minimum": 500, "maximum": 3600000, "description": "How often the label command runs.", "x-showIf": { "labelCommand": { "not": "" } } },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0C" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "BatteryWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Battery", "description": "Battery Status" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showPercentage": { "type": "boolean", "title": "Show percentage", "default": true },
            "lowThreshold": { "type": "integer", "title": "Low", "x-unit": "%", "default": 20, "minimum": 0, "maximum": 100, "description": "At or below this, the widget uses the low color." },
            "criticalThreshold": { "type": "integer", "title": "Critical", "x-unit": "%", "default": 10, "minimum": 0, "maximum": 100, "description": "At or below this, the widget uses the critical color." },
            "notify": { "type": "boolean", "title": "Notifications", "default": true, "description": "Send a notification when the battery becomes low or critical." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0D" },
            "chargingColor": { "type": "string", "title": "Charging background", "x-options": "colors", "default": "base0B" },
            "lowColor": { "type": "string", "title": "Low background", "x-options": "colors", "default": "base0C" },
            "criticalColor": { "type": "string", "title": "Critical background", "x-options": "colors", "default": "base08" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "SystemStatsWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "SystemStats", "description": "System Stats" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showCpu": { "type": "boolean", "title": "CPU usage", "default": true },
            "showMemory": { "type": "boolean", "title": "Memory", "default": true },
            "memoryFormat": { "type": "string", "title": "Memory format", "default": "percent", "enum": ["percent", "used"], "description": "Percentage of RAM in use, or GiB used.", "x-showIf": { "showMemory": true } },
            "showTemp": { "type": "boolean", "title": "CPU temperature", "default": true, "description": "Hidden on machines without a k10temp / coretemp / zenpower sensor." },
            "showGpu": { "type": "boolean", "title": "GPU usage", "default": false, "description": "AMD (sysfs) or NVIDIA (nvidia-smi). Hidden when no GPU is found." },
            "showDisk": { "type": "boolean", "title": "Disk usage", "default": false },
            "diskPath": { "type": "string", "title": "Disk path", "default": "/", "description": "Any path on the filesystem to report.", "x-showIf": { "showDisk": true } },
            "interval": { "type": "integer", "title": "Poll interval", "x-unit": "ms", "default": 2000, "minimum": 1000, "maximum": 60000 },
            "showPopout": { "type": "boolean", "title": "Popout", "default": true, "description": "Open live graphs of the shown metrics when hovered." },
            "warnThreshold": { "type": "integer", "title": "Warning at", "default": 90, "minimum": 1, "maximum": 100, "description": "Any usage (%) or temperature (°C) at or above this switches to the warning background." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0E" },
            "warnColor": { "type": "string", "title": "Warning background", "x-options": "colors", "default": "base08" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "KeyboardLayoutWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "KeyboardLayout", "description": "Keyboard Layout" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "format": { "type": "string", "title": "Format", "default": "short", "enum": ["short", "full"], "description": "Layout code (US) or full keymap name (English (US))." },
            "hideSingle": { "type": "boolean", "title": "Hide single layout", "default": false, "description": "Hide the widget when only one layout is configured." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0D" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "IdleInhibitorWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "IdleInhibitor", "description": "Idle Inhibitor" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showLabel": { "type": "boolean", "title": "Show label", "default": false },
            "activeColor": { "type": "string", "title": "Active background", "x-options": "colors", "default": "base0A" },
            "inactiveColor": { "type": "string", "title": "Inactive background", "x-options": "colors", "default": "base02" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "PrivacyWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Privacy", "description": "Privacy Indicators" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showMic": { "type": "boolean", "title": "Microphone", "default": true },
            "showScreen": { "type": "boolean", "title": "Screen share", "default": true },
            "showCamera": { "type": "boolean", "title": "Camera", "default": true },
            "ignoreApps": { "type": "string", "title": "Ignored apps", "default": "cava", "description": "Comma-separated app names that never trigger an indicator." },
            "activeColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base08" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "UpdatesWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Updates", "description": "Package Updates" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "includeAur": { "type": "boolean", "title": "Include AUR", "default": false, "description": "Also count outdated AUR packages." },
            "aurHelper": { "type": "string", "title": "AUR helper", "default": "paru", "enum": ["paru", "yay"], "x-showIf": { "includeAur": true } },
            "intervalMinutes": { "type": "integer", "title": "Check interval", "x-unit": "min", "default": 30, "minimum": 5, "maximum": 1440 },
            "hideWhenEmpty": { "type": "boolean", "title": "Hide when up to date", "default": true },
            "showPopout": { "type": "boolean", "title": "Popout", "default": true, "description": "List pending packages when hovered." },
            "terminal": { "type": "string", "title": "Terminal", "default": "kitty", "description": "Terminal the upgrade runs in (launched as `<terminal> -e ...`)." },
            "upgradeCommand": { "type": "string", "title": "Upgrade command", "default": "", "description": "Run on click. Empty uses `sudo pacman -Syu`, or the AUR helper with -Syu." },
            "manyThreshold": { "type": "integer", "title": "Many updates", "default": 50, "minimum": 1, "maximum": 1000, "description": "At or above this count, the widget uses the many-updates background." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0B" },
            "manyColor": { "type": "string", "title": "Many-updates background", "x-options": "colors", "default": "base09" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "WeatherWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Weather", "description": "Weather" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "location": { "type": "string", "title": "Location", "default": "", "description": "City name to look up. Empty uses your IP location." },
            "latitude": { "type": "string", "title": "Latitude", "default": "", "description": "Exact coordinates; with longitude, overrides the location lookup." },
            "longitude": { "type": "string", "title": "Longitude", "default": "" },
            "units": { "type": "string", "title": "Units", "default": "celsius", "enum": ["celsius", "fahrenheit"] },
            "intervalMinutes": { "type": "integer", "title": "Refresh interval", "x-unit": "min", "default": 15, "minimum": 5, "maximum": 1440 },
            "showCondition": { "type": "boolean", "title": "Show condition", "default": false, "description": "Show the condition (e.g. Rain) next to the temperature." },
            "showPopout": { "type": "boolean", "title": "Popout", "default": true, "description": "Show the forecast when hovered." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0C" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "SeparatorWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Separator", "description": "Separator / Spacer" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "style": { "type": "string", "title": "Style", "default": "line", "enum": ["line", "dot", "space"] },
            "size": { "type": "integer", "title": "Size", "default": 12, "minimum": 0, "maximum": 500, "x-unit": "px", "description": "Space it takes along the bar (px)." },
            "thickness": { "type": "integer", "title": "Thickness", "default": 2, "minimum": 1, "maximum": 10, "x-unit": "px", "description": "Line width (px); dots are twice this across.", "x-showIf": { "style": { "not": "space" } } },
            "length": { "type": "integer", "title": "Line length", "x-unit": "%", "default": 60, "minimum": 10, "maximum": 100, "description": "Share of the widget height the line spans.", "x-showIf": { "style": "line" } },
            "color": { "type": "string", "title": "Color", "x-options": "colors", "default": "base03", "x-showIf": { "style": { "not": "space" } } }
          }
        }
      }
    },
    "VolumeWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Volume", "description": "Volume" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showPercentage": { "type": "boolean", "title": "Show percentage", "default": false },
            "scrollStep": { "type": "integer", "title": "Scroll step", "x-unit": "%", "default": 5, "minimum": 1, "maximum": 25, "description": "Volume change per scroll notch." },
            "maxVolume": { "type": "integer", "title": "Maximum volume", "x-unit": "%", "default": 100, "minimum": 100, "maximum": 150, "description": "Highest volume scrolling and the mixer sliders allow." },
            "showPopout": { "type": "boolean", "title": "Popout", "default": true, "description": "Open the output mixer when hovered." },
            "middleCommand": { "type": "string", "title": "Middle-click command", "default": "pavucontrol", "description": "Shell command run on middle click. Empty does nothing." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0D" },
            "mutedColor": { "type": "string", "title": "Muted background", "x-options": "colors", "default": "base03" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "MicrophoneWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Microphone", "description": "Microphone" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showPercentage": { "type": "boolean", "title": "Show percentage", "default": false },
            "scrollStep": { "type": "integer", "title": "Scroll step", "x-unit": "%", "default": 5, "minimum": 1, "maximum": 25, "description": "Volume change per scroll notch." },
            "maxVolume": { "type": "integer", "title": "Maximum volume", "x-unit": "%", "default": 100, "minimum": 100, "maximum": 150, "description": "Highest volume scrolling and the mixer sliders allow." },
            "showPopout": { "type": "boolean", "title": "Popout", "default": true, "description": "Open the input mixer when hovered." },
            "middleCommand": { "type": "string", "title": "Middle-click command", "default": "pavucontrol", "description": "Shell command run on middle click. Empty does nothing." },
            "activeColor": { "type": "string", "title": "In-use background", "x-options": "colors", "default": "base08", "description": "Used while an app is recording from the microphone." },
            "hideWhenIdle": { "type": "boolean", "title": "Hide when idle", "default": false, "description": "Only show the widget while the microphone is in use or muted." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0D" },
            "mutedColor": { "type": "string", "title": "Muted background", "x-options": "colors", "default": "base03" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    },
    "BluetoothWidget": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": { "const": "Bluetooth", "description": "Bluetooth" },
        "visible": { "type": "boolean" },
        "layout": { "$ref": "#/definitions/WidgetLayout" },
        "properties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "showDevice": { "type": "boolean", "title": "Show device", "default": true, "description": "Show the connected device name (or count) next to the icon." },
            "showBattery": { "type": "boolean", "title": "Show battery", "default": true, "description": "Append the connected device's battery level, when it reports one.", "x-showIf": { "showDevice": true } },
            "hideWhenOff": { "type": "boolean", "title": "Hide when off", "default": false },
            "showPopout": { "type": "boolean", "title": "Popout", "default": true, "description": "Open the Bluetooth menu when hovered." },
            "middleCommand": { "type": "string", "title": "Middle-click command", "default": "", "description": "Shell command run on middle click (e.g. blueman-manager). Empty does nothing." },
            "backgroundColor": { "type": "string", "title": "Background", "x-options": "colors", "default": "base0D" },
            "connectedColor": { "type": "string", "title": "Connected background", "x-options": "colors", "default": "base0C" },
            "disabledColor": { "type": "string", "title": "Off background", "x-options": "colors", "default": "base03" },
            "foregroundColor": { "type": "string", "title": "Text color", "x-options": "colors", "default": "base00" }
          }
        }
      }
    }
  }
}
